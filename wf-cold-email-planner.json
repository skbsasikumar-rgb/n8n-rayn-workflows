{
  "name": "RAYN Cold Email Planner v1",
  "nodes": [
    {
      "parameters": {},
      "id": "manual-trigger",
      "name": "Manual Trigger",
      "type": "n8n-nodes-base.manualTrigger",
      "typeVersion": 1,
      "position": [
        0,
        0
      ]
    },
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "rayn-cold-email-planner",
        "responseMode": "onReceived",
        "options": {}
      },
      "id": "webhook-trigger",
      "name": "Webhook Trigger",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [
        0,
        220
      ]
    },
    {
      "parameters": {
        "url": "={{ (() => { const body = $json.body || {}; const force = (body.force || $json.force) === true; const rawIds = body.row_ids || $json.row_ids || []; const rowIds = (Array.isArray(rawIds) ? rawIds : String(rawIds || '').split(',')).map((value) => String(value).trim()).filter(Boolean); const base = '(status,eq,completed)~and(best_url,notblank)~and(duplicate_of_id,blank)'; const whereBase = force ? base : base + '~and(email_1_subject,blank)~and(automation_decision,blank)'; const where = rowIds.length ? whereBase + '~and(Id,in,' + rowIds.join(',') + ')' : whereBase; const limit = rowIds.length ? rowIds.length : Number(body.limit || $json.limit || 5); const sort = rowIds.length ? '' : '&sort=Id'; return 'https://nocodb-production-f802.up.railway.app/api/v1/db/data/noco/pb7f1zou786xyqc/mey3zgihq7o4at9?where=' + encodeURIComponent(where) + '&fields=Id,company_name,company_homepage_name,parent_company,best_url,canonical_domain,industry_guess,website_content,source_urls,services_detected,locations_detected,leadership_or_team_signals,contact_info_detected,structured_data_detected,selected_contact_name,selected_contact_title,selected_contact_role,selected_contact_email,validated_email,selected_contact_linkedin_url,do_not_contact,unsubscribe_status,email_source,attempt_count&limit=' + String(limit) + sort; })() }}",
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "nocoDbApiToken",
        "options": {}
      },
      "id": "get-outreach-rows",
      "name": "Get Outreach Rows",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        280,
        100
      ],
      "credentials": {
        "nocoDbApiToken": {
          "id": "d5Rz1LXeEoHMrZwo",
          "name": "NocoDB Token account"
        }
      }
    },
    {
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const rows = Array.isArray($json.list) ? $json.list : [];\nconst draftOnly = true;\nconst chunkSize = Math.max(1, Math.min(8, Number($json.body?.chunk_size || $json.chunk_size || 3)));\nconst concurrency = Number($json.body?.concurrency || $json.concurrency || 4);\nconst textFields = ['company_homepage_name', 'parent_company', 'best_url', 'canonical_domain', 'industry_guess', 'website_content', 'source_urls', 'services_detected', 'locations_detected', 'leadership_or_team_signals', 'contact_info_detected', 'structured_data_detected', 'selected_contact_name', 'selected_contact_title', 'selected_contact_role', 'selected_contact_email', 'selected_contact_linkedin_url', 'validated_email', 'unsubscribe_status', 'email_source'];\nconst normalizedRows = [];\nfor (const row of rows) {\n  const normalized = { ...row };\n  for (const field of textFields) normalized[field] = String(normalized[field] || '').trim();\n  normalized.unsubscribe_status = normalized.unsubscribe_status || 'active';\n  normalized.do_not_contact = normalized.do_not_contact === true || String(normalized.do_not_contact).toLowerCase() === 'true';\n  normalizedRows.push({ ...normalized, draft_only: draftOnly });\n}\nconst output = [];\nfor (let index = 0; index < normalizedRows.length; index += chunkSize) {\n  output.push({ json: { rows: normalizedRows.slice(index, index + chunkSize), concurrency, copy_qa_mode: Boolean($json.body?.copy_qa_mode || $json.copy_qa_mode), chunk_index: output.length + 1, chunk_size: chunkSize, total_rows: normalizedRows.length } });\n}\nreturn output.length ? output : [{ json: { rows: [], concurrency, copy_qa_mode: false, chunk_index: 1, chunk_size: chunkSize, total_rows: 0 } }];"
      },
      "id": "rows-to-items",
      "name": "Rows To Items",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        560,
        100
      ]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ String($env.CRAWL4AI_SCRAPER_URL || 'https://n8n-rayn-workflows-production.up.railway.app/scrape').replace(/\\/scrape$/, '/outreach-plan-batch') }}",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify($json) }}",
        "options": {
          "timeout": 900000
        }
      },
      "id": "outreach-plan",
      "name": "Generate Outreach Plan",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        840,
        100
      ]
    },
    {
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "function parseFlags(value) {\n  try { return Array.isArray(value) ? value : JSON.parse(value || '[]'); } catch (error) { return value ? [String(value)] : []; }\n}\nfunction auditFromPatch(patch, prior = {}) {\n  return {\n    row_id: patch.Id || prior.row_id || '',\n    company_name: prior.company_name || patch.company_name || '',\n    pressure_type: patch.pressure_type || prior.pressure_type || '',\n    hia_service_type_guess: patch.hia_service_type_guess || prior.hia_service_type_guess || '',\n    hia_timeline_batch_guess: patch.hia_timeline_batch_guess || prior.hia_timeline_batch_guess || '',\n    funding_status: patch.funding_status || prior.funding_status || '',\n    automation_decision: patch.automation_decision || prior.automation_decision || '',\n    automation_decision_reason: patch.automation_decision_reason || prior.automation_decision_reason || '',\n    automation_blockers_json: parseFlags(patch.automation_blockers_json || prior.automation_blockers_json),\n    automation_advisory_flags_json: parseFlags(patch.automation_advisory_flags_json || prior.automation_advisory_flags_json),\n    contact_send_mode: patch.contact_send_mode || prior.contact_send_mode || '',\n    contact_identity_confidence: patch.contact_identity_confidence || prior.contact_identity_confidence || '',\n    email_2_mode: patch.email_2_mode || prior.email_2_mode || patch.email_3_mode || prior.email_3_mode || '',\n    funding_followup_mode: patch.funding_followup_mode || prior.funding_followup_mode || patch.email_2_mode || prior.email_2_mode || patch.email_3_mode || prior.email_3_mode || '',\n    email_3_mode: patch.email_3_mode || prior.email_3_mode || patch.email_2_mode || prior.email_2_mode || '',\n    enrichment_quality_score: patch.enrichment_quality_score || prior.enrichment_quality_score || 0,\n    enrichment_quality_flags: parseFlags(patch.enrichment_quality_flags || prior.enrichment_quality_flags),\n    copy_brief_quality_score: patch.copy_brief_quality_score || prior.copy_brief_quality_score || 0,\n    copy_brief_quality_flags: parseFlags(patch.copy_brief_quality_flags || prior.copy_brief_quality_flags),\n    severe_email_flags: parseFlags(patch.severe_email_flags || prior.severe_email_flags),\n    final_send_gate_passed: patch.final_send_gate_passed || prior.final_send_gate_passed || false,\n    email_quality_flags: parseFlags(patch.email_quality_flags || prior.email_quality_flags),\n    email_1_subject: patch.email_1_subject || prior.email_1_subject || '',\n    email_1_body: patch.email_1_body || prior.email_1_body || '',\n    email_2_subject: patch.email_2_subject || prior.email_2_subject || '',\n    email_2_body: patch.email_2_body || prior.email_2_body || '',\n    email_3_subject: patch.email_3_subject || prior.email_3_subject || '',\n    email_3_body: patch.email_3_body || prior.email_3_body || '',\n    email_4_subject: patch.email_4_subject || prior.email_4_subject || '',\n    email_4_body: patch.email_4_body || prior.email_4_body || '',\n  };\n}\nconst inputItems = typeof items !== 'undefined' ? items : (typeof $input !== 'undefined' ? [$input.first()] : []);\nconst patches = [];\nconst audits = [];\nconst errors = [];\nlet requested = 0;\nlet maxConcurrency = 0;\nfor (const item of inputItems) {\n  const body = item?.json || {};\n  const results = Array.isArray(body.results) ? body.results : [];\n  requested += Number(body.requested || results.length || 0);\n  maxConcurrency = Math.max(maxConcurrency, Number(body.concurrency || 0));\n  if (Array.isArray(body.patches)) patches.push(...body.patches);\n  if (Array.isArray(body.audits)) audits.push(...body.audits);\n  if (Array.isArray(body.errors)) errors.push(...body.errors);\n  if (body.patch && body.patch.Id) {\n    patches.push(body.patch);\n    audits.push(auditFromPatch(body.patch, body.audit_report || {}));\n  }\n  if (!Array.isArray(body.patches) && results.length) {\n    for (const result of results) {\n      if (result.patch && result.patch.Id) {\n        patches.push(result.patch);\n        audits.push(auditFromPatch(result.patch, result.audit_report || {}));\n      } else {\n        errors.push({ row_id: result.row_id || '', error: result.error || 'missing_patch' });\n      }\n    }\n  }\n}\nreturn [{ json: { patches, audits, errors, count: patches.length, requested: requested || patches.length, concurrency: maxConcurrency } }];"
      },
      "id": "collect-patches",
      "name": "Collect NocoDB Patches",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2180,
        100
      ]
    },
    {
      "parameters": {
        "method": "PATCH",
        "url": "https://nocodb-production-f802.up.railway.app/api/v1/db/data/bulk/noco/pb7f1zou786xyqc/mey3zgihq7o4at9",
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "nocoDbApiToken",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify($json.patches) }}",
        "options": {
          "timeout": 900000
        }
      },
      "id": "patch-drafts",
      "name": "Patch Draft Fields",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        2460,
        100
      ],
      "credentials": {
        "nocoDbApiToken": {
          "id": "d5Rz1LXeEoHMrZwo",
          "name": "NocoDB Token account"
        }
      }
    }
  ],
  "connections": {
    "Manual Trigger": {
      "main": [
        [
          {
            "node": "Get Outreach Rows",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Webhook Trigger": {
      "main": [
        [
          {
            "node": "Get Outreach Rows",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Get Outreach Rows": {
      "main": [
        [
          {
            "node": "Rows To Items",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Rows To Items": {
      "main": [
        [
          {
            "node": "Generate Outreach Plan",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Generate Outreach Plan": {
      "main": [
        [
          {
            "node": "Collect NocoDB Patches",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Collect NocoDB Patches": {
      "main": [
        [
          {
            "node": "Patch Draft Fields",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "saveDataSuccessExecution": "all"
  },
  "staticData": null,
  "tags": []
}

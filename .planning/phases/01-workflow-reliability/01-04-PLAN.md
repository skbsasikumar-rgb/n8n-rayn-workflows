---
phase: 01-workflow-reliability
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified: [wf-latest.json]
autonomous: true
requirements: [FIX-04, FIX-05, FIX-06]

must_haves:
  truths:
    - "Hunter is called when a lead has a name but no email (OR logic, not AND)"
    - "A lead whose No2Bounce validation is still processing after retries is written as verification_timeout, not forced to valid/invalid"
    - "wf-latest paginates all NocoDB GET requests using pageInfo.isLastPage loop -- no hardcoded limit=10000"
  artifacts:
    - path: "wf-latest.json"
      provides: "OR combinator on IF No Contact, verification_timeout status on slow validations, pagination loop for NocoDB reads"
      contains: "verification_timeout"
  key_links:
    - from: "IF No Contact node"
      to: "Hunter HTTP Request node"
      via: "OR combinator triggers Hunter when name OR email is empty"
      pattern: "combinator.*or"
    - from: "Parse Poll node"
      to: "NocoDB PATCH verification_timeout"
      via: "When isReady is false after final poll, writes verification_timeout instead of forced verdict"
      pattern: "verification_timeout"
    - from: "Paginate Pending Leads Code node"
      to: "NocoDB API"
      via: "While loop until pageInfo.isLastPage === true"
      pattern: "isLastPage"
---

<objective>
Fix contact fallback logic (AND to OR), replace forced No2Bounce verdict with verification_timeout status, and add NocoDB pagination to all wf-latest GET requests.

Purpose: FIX-04 ensures Hunter is triggered when ANY contact field is missing (not only when BOTH are missing). FIX-05 Part A prevents false valid/invalid verdicts on slow No2Bounce validations. FIX-06 ensures wf-latest sees all pending rows even when the NocoDB table exceeds the default page size.

Output: Modified wf-latest.json with OR combinator, verification_timeout logic, and pagination loops.
</objective>

<execution_context>
@/Users/sasikumar/Documents/n8n/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sasikumar/Documents/n8n/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-workflow-reliability/01-CONTEXT.md
@.planning/phases/01-workflow-reliability/01-RESEARCH.md
@.planning/phases/01-workflow-reliability/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix IF No Contact to OR logic + fix Parse Poll forced verdict (FIX-04 per D-10, FIX-05 Part A per D-02)</name>
  <files>wf-latest.json</files>
  <read_first>
    wf-latest.json
    .planning/phases/01-workflow-reliability/01-RESEARCH.md
  </read_first>
  <action>
Two targeted fixes in wf-latest.json:

**Fix A -- Contact Fallback OR Logic (FIX-04, per D-10):**

Locate the IF No Contact node (id: `f8e0ec97-1d87-46b4-9160-ebebdba9b5c8`).

In its parameters, find:
```json
"combinator": "and"
```

Change to:
```json
"combinator": "or"
```

This is a single field change. The two conditions (first_name empty, email empty) remain identical. After this fix, Hunter is triggered when name OR email is empty, not only when BOTH are empty.

No other changes to this node. The conditions themselves stay the same -- only the combinator changes.

**Fix B -- No2Bounce Verification Timeout (FIX-05 Part A, per D-02):**

Locate the Parse Poll 4 node (id: `e51b9dfe-196f-4298-baea-2a7e94ff9fd9`) or whichever is the final poll parse node in the No2Bounce polling chain.

Find the forced verdict line in its jsCode:
```javascript
const status = isValid ? 'valid' : 'invalid';
```

Replace with:
```javascript
const status = isReady
  ? (isValid ? 'valid' : 'invalid')
  : 'verification_timeout';
```

This ensures that if `isReady` is false (No2Bounce is still processing), the lead gets `verification_timeout` status instead of a forced verdict.

After the Code node, verify the downstream wiring:
- If the enrichment chain has a NocoDB PATCH that writes the email validation status, ensure it can handle `verification_timeout` as a value. The PATCH node likely uses `$json.status` or similar -- confirm it passes through whatever the Code node returns.
- The lead should continue through the rest of the enrichment chain (it does not stop on verification_timeout -- it just records that the email was not validated in time).
- The separate retry workflow (Plan 05) will pick up `verification_timeout` leads later.

If there is an IF node after Parse Poll 4 that branches on `valid` vs `invalid`, add a third branch or ensure `verification_timeout` follows the same path as the existing fallback. The lead should still proceed to final status update -- the only difference is the `email_status` field value.
  </action>
  <acceptance_criteria>
    - The IF No Contact node (id f8e0ec97) has "combinator": "or" in its parameters (not "and")
    - The final poll parse node's jsCode contains the string "verification_timeout"
    - The final poll parse node's jsCode contains "isReady" check before assigning status
    - The jsCode does NOT contain a bare `isValid ? 'valid' : 'invalid'` without an isReady guard
    - wf-latest.json is valid JSON
  </acceptance_criteria>
  <verify>
    <automated>cat wf-latest.json | python3 -c "
import sys, json
d = json.load(sys.stdin)
# Check OR combinator
contact_nodes = [n for n in d['nodes'] if 'No Contact' in n.get('name','')]
assert len(contact_nodes) >= 1, 'IF No Contact node not found'
for cn in contact_nodes:
    conds = cn.get('parameters',{}).get('conditions',{})
    combo = conds.get('combinator', cn.get('parameters',{}).get('combinator',''))
    # combinator may be nested
    params_str = json.dumps(cn.get('parameters',{}))
    assert '\"or\"' in params_str or '\"combinator\": \"or\"' in params_str, f'IF No Contact still uses AND combinator'
# Check verification_timeout
all_code = json.dumps(d)
assert 'verification_timeout' in all_code, 'verification_timeout status not found in any node'
print('PASS: OR combinator and verification_timeout confirmed')
"</automated>
  </verify>
  <done>IF No Contact uses OR combinator (Hunter triggered on name OR email empty). Final poll parse writes verification_timeout when isReady is false instead of forcing valid/invalid.</done>
</task>

<task type="auto">
  <name>Task 2: Add NocoDB pagination to all wf-latest GET requests (FIX-06, per D-11)</name>
  <files>wf-latest.json</files>
  <read_first>
    wf-latest.json
    .planning/phases/01-workflow-reliability/01-RESEARCH.md
  </read_first>
  <action>
Replace all hardcoded `limit=10000` NocoDB GET requests in wf-latest.json with paginating Code nodes that loop until `pageInfo.isLastPage === true`.

From RESEARCH.md, the affected nodes are:
1. **Read All Rows** (node id: `90b8fd61`) -- fetches all rows (used for the initial pending lead read)
2. **Read Column E** (node id: `60be1b05`) -- fetches rows for column E processing

NOTE: Plan 02 may have already replaced Read All Rows with "Get Pending IDs" (which only fetches 5 pending IDs, not all rows). If so, Read All Rows is no longer in the workflow and only Read Column E (or any remaining `limit=10000` GET nodes) need pagination.

For EACH remaining NocoDB GET node that uses `limit=10000` or any hardcoded high limit:

Replace the HTTP Request node with a Code node that paginates. Name it "Paginate {Original Node Name}" (e.g., "Paginate Read Column E").

Code node (runOnceForAllItems):
```javascript
const baseUrl = 'https://nocodb-production-f802.up.railway.app';
const projectId = 'pb7f1zou786xyqc';
const tableId = 'mey3zgihq7o4at9';
const token = 'SqqM8YcDuzXGijg0oY7PvqTcPX5H-r2sYqbAOWTN';
const pageSize = 1000;
// Adjust 'where' clause based on what the original node filtered:
const where = encodeURIComponent('(status,eq,pending)'); // or remove if no filter

let page = 1;
let allRecords = [];
let isLastPage = false;

while (!isLastPage) {
  const url = `${baseUrl}/api/v1/db/data/noco/${projectId}/${tableId}` +
    `?where=${where}&limit=${pageSize}&page=${page}`;
  const response = await $http.request({
    method: 'GET',
    url,
    headers: { 'xc-token': token }
  });
  allRecords = allRecords.concat(response.list || []);
  isLastPage = response.pageInfo?.isLastPage ?? true;
  page += 1;
}

return allRecords.map(r => ({ json: r }));
```

For each replacement:
1. Note the original node's `where` filter and `fields` parameter from its URL
2. Replicate the same filter in the pagination Code node
3. Wire the Code node's output to whatever the original HTTP Request node connected to
4. Wire the Code node's input from whatever previously connected to the original HTTP Request node
5. Remove the old HTTP Request node from the nodes array and connections

If `$http.request()` is not available (see VALIDATION.md Wave 0 gap), use this fallback approach: Keep the HTTP Request node but set `limit=1000` and add a loop structure using Loop Over Items + IF node checking if the response count < pageSize. This is more verbose but works without `$http.request()`.

IMPORTANT: After pagination, the downstream Parse node (e.g., "Parse Read Items") that extracted `$json.list` from the HTTP response is no longer needed -- the Code node already returns individual items. Remove or bypass the Parse node, or update it to pass through items directly.

Scan the entire wf-latest.json for any remaining `limit=10000` strings. Every occurrence must be replaced with pagination.
  </action>
  <acceptance_criteria>
    - wf-latest.json contains NO occurrence of the string "limit=10000" (all hardcoded limits replaced)
    - At least one Code node contains the string "isLastPage" (pagination loop implemented)
    - The pagination Code node(s) use pageSize = 1000 (matching DB_QUERY_LIMIT_DEFAULT from INFRA-02)
    - The pagination Code node(s) contain "page += 1" or "page++" (incrementing page counter)
    - wf-latest.json is valid JSON
  </acceptance_criteria>
  <verify>
    <automated>cat wf-latest.json | python3 -c "
import sys, json
d = json.load(sys.stdin)
raw = json.dumps(d)
assert 'limit=10000' not in raw, 'Still has hardcoded limit=10000'
assert 'isLastPage' in raw, 'No pagination loop found (missing isLastPage)'
assert 'pageSize' in raw or 'page_size' in raw or '1000' in raw, 'No pageSize reference found'
print('PASS: No limit=10000 found, pagination loop present')
"</automated>
  </verify>
  <done>All NocoDB GET requests in wf-latest.json use pagination loops (page until isLastPage===true). No hardcoded limit=10000 remains. Page size set to 1000 matching DB_QUERY_LIMIT_DEFAULT.</done>
</task>

</tasks>

<verification>
- wf-latest.json is valid JSON
- IF No Contact uses OR combinator
- Final poll parse writes verification_timeout when isReady is false
- No occurrence of limit=10000 in wf-latest.json
- Pagination loop with isLastPage check exists for all NocoDB GET requests
</verification>

<success_criteria>
- Hunter is called when Anymail returns a name but no email (OR logic confirmed)
- A lead whose No2Bounce validation is still processing after the final poll is written as verification_timeout (not valid or invalid)
- When NocoDB leads table exceeds 100 rows, all rows in status=pending are visible to wf-latest on the next run
</success_criteria>

<output>
After completion, create `.planning/phases/01-workflow-reliability/01-04-SUMMARY.md`
</output>

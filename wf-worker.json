{
  "name": "RAYN URL Picker Worker v1",
  "id": "BQEa6M2pKYmuEYMV",
  "active": true,
  "settings": {
    "executionOrder": "v1"
  },
  "nodes": [
    {
      "id": "manual-trigger",
      "name": "Manual Trigger",
      "type": "n8n-nodes-base.manualTrigger",
      "typeVersion": 1,
      "position": [
        220,
        300
      ],
      "parameters": {}
    },
    {
      "id": "webhook-trigger",
      "name": "Webhook Trigger",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [
        220,
        460
      ],
      "parameters": {
        "httpMethod": "POST",
        "path": "rayn-url-picker-v1",
        "responseMode": "onReceived",
        "options": {}
      },
      "webhookId": "a19a3db8-b7bc-4cb6-9a8d-89d2bb35a2da"
    },
    {
      "id": "webhook-batch-trigger",
      "name": "Webhook Batch Trigger",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [
        220,
        620
      ],
      "parameters": {
        "httpMethod": "POST",
        "path": "rayn-url-picker-batch",
        "responseMode": "onReceived",
        "options": {}
      },
      "webhookId": "31ef8c51-921c-49f8-87aa-fd177ec4c7ea"
    },
    {
      "id": "get-blank-url-picked",
      "name": "Get Blank URL Picked Rows",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        480,
        300
      ],
      "parameters": {
        "method": "GET",
        "url": "={{ (() => { const body = $json.body || {}; const retryFailed = body.retry_failed === true || $json.retry_failed === true || String(body.retry_failed || $json.retry_failed || '').trim().toLowerCase() === 'true'; const limit = Math.max(1, Math.min(25, Number(body.discovery_limit || body.limit || $json.discovery_limit || $json.limit || $env.RAYN_URL_PICKER_DISCOVERY_LIMIT || 5))); const staleMinutes = Math.max(5, Number($env.RAYN_STALE_PROCESSING_MINUTES || 15)); const staleCutoff = encodeURIComponent(new Date(Date.now() - staleMinutes * 60 * 1000).toISOString()); const rediscoveryWhere = '(status,eq,skipped)~and(status_reason,eq,skipped_url_validation_failed)'; const where = retryFailed ? '(company_name,notblank)~and(' + rediscoveryWhere + ')' : '(company_name,notblank)~and(url_picked,blank)~and((status,blank)~or(status,eq,pending)~or(status,eq,failed_retryable)~or((status,eq,processing)~and(processing_started_at,lt,' + staleCutoff + ')))'; return 'https://nocodb-production-f802.up.railway.app/api/v1/db/data/noco/pb7f1zou786xyqc/mey3zgihq7o4at9?where=' + where + '&fields=Id,company_name,url_picked,homepage_root_url,best_url,canonical_domain,status,attempt_count,run_id,processing_started_at,processing_finished_at,last_attempted_at,status_reason,error_type,error_message,retry_eligible,source_row_created_at,source_row_updated_at,CreatedAt,UpdatedAt&limit=' + limit; })() }}",
        "sendHeaders": false,
        "options": {
          "timeout": 60000
        },
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "nocoDbApiToken"
      },
      "credentials": {
        "nocoDbApiToken": {
          "id": "d5Rz1LXeEoHMrZwo",
          "name": "NocoDB Token account"
        }
      }
    },
    {
      "id": "get-enrichment-rows",
      "name": "Get Enrichment Rows",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        480,
        620
      ],
      "parameters": {
        "method": "GET",
        "url": "={{ (() => { const body = $json.body || {}; const retryFailed = body.retry_failed === true || $json.retry_failed === true || String(body.retry_failed || $json.retry_failed || '').trim().toLowerCase() === 'true'; const limitValue = retryFailed ? (body.retry_limit || body.enrichment_limit || body.limit || $json.retry_limit || $json.enrichment_limit || $json.limit || $env.RAYN_URL_PICKER_RETRY_LIMIT || 2) : (body.enrichment_limit || body.limit || $json.enrichment_limit || $json.limit || $env.RAYN_URL_PICKER_ENRICHMENT_LIMIT || 5); const limit = Math.max(1, Math.min(25, Number(limitValue))); const staleMinutes = Math.max(5, Number($env.RAYN_STALE_PROCESSING_MINUTES || 15)); const staleCutoff = encodeURIComponent(new Date(Date.now() - staleMinutes * 60 * 1000).toISOString()); const retryWhere = '((((status,eq,failed_retryable)~or(status,eq,failed))~and(retry_eligible,eq,true))~or(status,eq,completed)~and(automation_decision,eq,retry_enrichment_once))'; const statusWhere = retryFailed ? retryWhere : '((status,eq,url_picked)~or((status,eq,processing)~and(processing_started_at,lt,' + staleCutoff + ')))'; const manualOverrideWhere = '(manual_url_override,notblank)~and(status,eq,skipped)~and((status_reason,eq,no_official_url_found)~or(status_reason,eq,skipped_url_validation_failed))'; const where = '(company_name,notblank)~and((url_picked,notblank)~and' + statusWhere + '~or' + manualOverrideWhere + ')'; return 'https://nocodb-production-f802.up.railway.app/api/v1/db/data/noco/pb7f1zou786xyqc/mey3zgihq7o4at9?where=' + where + '&fields=Id,company_name,url_picked,manual_url_override,homepage_root_url,best_url,canonical_domain,status,attempt_count,run_id,processing_started_at,processing_finished_at,last_attempted_at,status_reason,error_type,error_message,retry_eligible,automation_decision,automation_decision_reason,pressure_type,primary_email_track,secondary_email_track,contact_search_status,validated_email,selected_contact_email,email_1_subject,email_1_body,email_2_subject,email_2_body,email_3_subject,email_3_body,email_quality_flags,severe_email_flags,send_provider,send_status,sequence_status,instantly_sync_status,source_row_created_at,source_row_updated_at,CreatedAt,UpdatedAt&limit=' + limit + '&sort=Id'; })() }}",
        "sendHeaders": false,
        "options": {
          "timeout": 60000
        },
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "nocoDbApiToken"
      },
      "credentials": {
        "nocoDbApiToken": {
          "id": "d5Rz1LXeEoHMrZwo",
          "name": "NocoDB Token account"
        }
      }
    },
    {
      "id": "webhook-to-item",
      "name": "Webhook To Item",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        480,
        460
      ],
      "parameters": {
        "mode": "runOnceForEachItem",
        "jsCode": "const payload = ($json.body && Object.keys($json.body).length ? $json.body : $json) || {};\nconst idValue = payload.Id ?? payload.id ?? '';\nconst companyName = String(payload.company_name || '').trim();\nif (!idValue || !companyName) {\n  throw new Error('Webhook payload requires Id and company_name');\n}\nconst urlPicked = String(payload.url_picked || '').trim();\nreturn {\n  json: {\n    Id: idValue,\n    company_name: companyName,\n    stage_mode: String(payload.stage_mode || '').trim(),\n    ...(urlPicked ? { url_picked: urlPicked } : {}),\n    homepage_root_url: String(payload.homepage_root_url || '').trim(),\n    best_url: String(payload.best_url || '').trim(),\n    canonical_domain: String(payload.canonical_domain || '').trim(),\n    excluded_url_domain: String(payload.excluded_url_domain || '').trim(),\n    status: 'pending',\n    attempt_count: String(payload.attempt_count || '0').trim() || '0',\n    run_id: String(payload.run_id || '').trim(),\n    processing_started_at: String(payload.processing_started_at || '').trim(),\n    processing_finished_at: String(payload.processing_finished_at || '').trim(),\n    last_attempted_at: String(payload.last_attempted_at || '').trim(),\n    status_reason: String(payload.status_reason || '').trim(),\n    error_type: String(payload.error_type || '').trim(),\n    error_message: String(payload.error_message || '').trim(),\n    retry_eligible: String(payload.retry_eligible || '').trim(),\n    source_row_created_at: String(payload.source_row_created_at || payload.CreatedAt || '').trim(),\n    source_row_updated_at: String(payload.source_row_updated_at || payload.UpdatedAt || '').trim(),\n    CreatedAt: String(payload.CreatedAt || '').trim(),\n    UpdatedAt: String(payload.UpdatedAt || '').trim()\n  }\n};"
      }
    },
    {
      "id": "rows-to-items",
      "name": "Rows To Items",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        740,
        300
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const rows = Array.isArray($json.list) ? $json.list : [];\nconst staleMinutes = Math.max(5, Number($env.RAYN_STALE_PROCESSING_MINUTES || 15));\nconst maxRetryAttempts = Math.max(1, Number($env.RAYN_MAX_RETRY_ATTEMPTS || 2));\nfunction isStaleProcessing(row) {\n  const status = String(row.status || '').trim();\n  if (status !== 'processing') return false;\n  const startedAt = Date.parse(String(row.processing_started_at || row.last_attempted_at || row.UpdatedAt || ''));\n  if (!Number.isFinite(startedAt)) return true;\n  return Date.now() - startedAt > staleMinutes * 60 * 1000;\n}\nfunction attemptCount(row) {\n  const attempts = Number.parseInt(String(row.attempt_count || '0'), 10);\n  return Number.isFinite(attempts) ? attempts : 0;\n}\nfunction domainFromUrl(value) {\n  const raw = String(value || '').trim();\n  if (!raw) return '';\n  try {\n    const parsed = new URL(/^[a-z][a-z0-9+.-]*:\\/\\//i.test(raw) ? raw : 'https://' + raw);\n    return parsed.hostname.toLowerCase().replace(/^www\\./, '');\n  } catch {\n    return '';\n  }\n}\nfunction excludedUrlDomain(row) {\n  return String(row.status_reason || '').trim() === 'skipped_url_validation_failed'\n    ? (String(row.canonical_domain || '').trim().toLowerCase() || domainFromUrl(row.url_picked || row.best_url || row.homepage_root_url))\n    : '';\n}\nfunction isRetryable(row) {\n  const status = String(row.status || '').trim();\n  if (attemptCount(row) >= maxRetryAttempts) return false;\n  return status === 'failed_retryable' || (status === 'failed' && String(row.retry_eligible || '').trim() === 'true');\n}\nfunction isUrlRediscovery(row) {\n  const status = String(row.status || '').trim();\n  const reason = String(row.status_reason || '').trim();\n  const rediscoveryCap = Math.max(maxRetryAttempts, Number($env.RAYN_MAX_URL_REDISCOVERY_ATTEMPTS || 6));\n  return status === 'skipped'\n    && reason === 'skipped_url_validation_failed'\n    && String(row.url_picked || row.best_url || row.homepage_root_url || '').trim()\n    && attemptCount(row) < rediscoveryCap;\n}\nfunction isPendingOrNew(row) {\n  const status = String(row.status || '').trim();\n  return !status || status === 'pending' || isRetryable(row) || isUrlRediscovery(row) || isStaleProcessing(row);\n}\nreturn rows\n  .filter((row) => row && row.Id && String(row.company_name || '').trim() && isPendingOrNew(row))\n  .map((row, index) => ({\n    json: {\n      Id: row.Id,\n      company_name: String(row.company_name).trim(),\n      url_picked: String(row.url_picked || '').trim(),\n      homepage_root_url: String(row.homepage_root_url || '').trim(),\n      best_url: String(row.best_url || '').trim(),\n      canonical_domain: String(row.canonical_domain || '').trim(),\n      excluded_url_domain: excludedUrlDomain(row),\n      status: 'pending',\n      attempt_count: String(row.attempt_count || '').trim() || '0',\n      run_id: String(row.run_id || '').trim(),\n      processing_started_at: String(row.processing_started_at || '').trim(),\n      processing_finished_at: String(row.processing_finished_at || '').trim(),\n      last_attempted_at: String(row.last_attempted_at || '').trim(),\n      status_reason: String(row.status_reason || '').trim(),\n      error_type: String(row.error_type || '').trim(),\n      error_message: String(row.error_message || '').trim(),\n      retry_eligible: String(row.retry_eligible || '').trim(),\n      source_row_created_at: String(row.source_row_created_at || row.CreatedAt || '').trim(),\n      source_row_updated_at: String(row.source_row_updated_at || row.UpdatedAt || '').trim(),\n      CreatedAt: String(row.CreatedAt || '').trim(),\n      UpdatedAt: String(row.UpdatedAt || '').trim()\n    },\n    pairedItem: { item: index }\n  }));"
      }
    },
    {
      "id": "rows-to-enrichment-items",
      "name": "Rows To Enrichment Items",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        740,
        620
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const rows = Array.isArray($json.list) ? $json.list : [];\nconst staleMinutes = Math.max(5, Number($env.RAYN_STALE_PROCESSING_MINUTES || 15));\nconst maxRetryAttempts = Math.max(1, Number($env.RAYN_MAX_ENRICHMENT_ATTEMPTS || 4));\nfunction attemptCount(row) {\n  const attempts = Number.parseInt(String(row.attempt_count || '0'), 10);\n  return Number.isFinite(attempts) ? attempts : 0;\n}\nfunction normalizeManualUrl(value) {\n  const raw = String(value || '').trim();\n  if (!raw) return '';\n  const lower = raw.toLowerCase();\n  if (lower.startsWith('http://') || lower.startsWith('https://')) return raw;\n  let withoutLeadingSlashes = raw;\n  while (withoutLeadingSlashes.startsWith('/')) {\n    withoutLeadingSlashes = withoutLeadingSlashes.slice(1);\n  }\n  return 'https://' + withoutLeadingSlashes;\n}\nfunction canonicalDomain(url) {\n  let value = String(url || '').trim().toLowerCase();\n  if (value.startsWith('http://')) value = value.slice(7);\n  if (value.startsWith('https://')) value = value.slice(8);\n  value = value.split('/')[0].split(':')[0].trim();\n  if (value.startsWith('www.')) value = value.slice(4);\n  return value;\n}\nfunction isStaleProcessing(row) {\n  const status = String(row.status || '').trim();\n  if (status !== 'processing') return false;\n  const startedAt = Date.parse(String(row.processing_started_at || row.last_attempted_at || row.UpdatedAt || ''));\n  if (!Number.isFinite(startedAt)) return true;\n  return Date.now() - startedAt > staleMinutes * 60 * 1000;\n}\nfunction isWeakEnrichmentRetry(row) {\n  return String(row.status || '').trim() === 'completed'\n    && String(row.automation_decision || '').trim() === 'retry_enrichment_once';\n}\nfunction isRetryableFailure(row) {\n  const status = String(row.status || '').trim();\n  return (status === 'failed_retryable' || status === 'failed')\n    && String(row.retry_eligible || '').trim() === 'true'\n    && attemptCount(row) < maxRetryAttempts;\n}\nfunction isManualUrlOverride(row) {\n  const reason = String(row.status_reason || '').trim();\n  return String(row.status || '').trim() === 'skipped'\n    && ['no_official_url_found', 'skipped_url_validation_failed'].includes(reason)\n    && !!normalizeManualUrl(row.manual_url_override);\n}\nfunction isAlreadyInSendPipeline(row) {\n  const provider = String(row.send_provider || '').trim();\n  const syncStatus = String(row.instantly_sync_status || '').trim();\n  const sendStatus = String(row.send_status || '').trim();\n  const sequenceStatus = String(row.sequence_status || '').trim();\n  return provider === 'instantly'\n    || syncStatus === 'synced'\n    || ['queued', 'sent', 'suppressed'].includes(sendStatus)\n    || ['queued', 'email_1_sent', 'email_2_scheduled', 'completed', 'replied', 'suppressed'].includes(sequenceStatus);\n}\nfunction shouldEnrich(row) {\n  if (isAlreadyInSendPipeline(row)) return false;\n  const status = String(row.status || '').trim();\n  return status === 'url_picked' || isRetryableFailure(row) || isStaleProcessing(row) || isWeakEnrichmentRetry(row) || isManualUrlOverride(row);\n}\nreturn rows\n  .filter((row) => row && row.Id && String(row.company_name || '').trim() && String(row.url_picked || row.manual_url_override || '').trim() && shouldEnrich(row))\n  .map((row, index) => {\n    const manualOverride = isManualUrlOverride(row);\n    const manualUrl = normalizeManualUrl(row.manual_url_override);\n    const pickedUrl = manualOverride ? manualUrl : String(row.url_picked || '').trim();\n    return {\n      json: {\n        Id: row.Id,\n        company_name: String(row.company_name).trim(),\n        url_picked: pickedUrl,\n        manual_url_override: manualUrl,\n        allow_cross_domain_redirect: Boolean(manualUrl),\n        homepage_root_url: manualOverride ? manualUrl : String(row.homepage_root_url || '').trim(),\n        best_url: manualOverride ? manualUrl : String(row.best_url || '').trim(),\n        canonical_domain: manualOverride ? canonicalDomain(manualUrl) : String(row.canonical_domain || '').trim(),\n        status: String(row.status || '').trim(),\n        attempt_count: String(row.attempt_count || '').trim() || '0',\n        run_id: String(row.run_id || '').trim(),\n        processing_started_at: String(row.processing_started_at || '').trim(),\n        processing_finished_at: String(row.processing_finished_at || '').trim(),\n        last_attempted_at: String(row.last_attempted_at || '').trim(),\n        status_reason: String(row.status_reason || '').trim(),\n        error_type: String(row.error_type || '').trim(),\n        error_message: String(row.error_message || '').trim(),\n        retry_eligible: String(row.retry_eligible || '').trim(),\n        automation_decision: String(row.automation_decision || '').trim(),\n        automation_decision_reason: String(row.automation_decision_reason || '').trim(),\n        pressure_type: String(row.pressure_type || '').trim(),\n        primary_email_track: String(row.primary_email_track || '').trim(),\n        secondary_email_track: String(row.secondary_email_track || '').trim(),\n        contact_search_status: String(row.contact_search_status || '').trim(),\n        validated_email: String(row.validated_email || '').trim(),\n        selected_contact_email: String(row.selected_contact_email || '').trim(),\n        send_provider: String(row.send_provider || '').trim(),\n        send_status: String(row.send_status || '').trim(),\n        sequence_status: String(row.sequence_status || '').trim(),\n        instantly_sync_status: String(row.instantly_sync_status || '').trim(),\n        source_row_created_at: String(row.source_row_created_at || row.CreatedAt || '').trim(),\n        source_row_updated_at: String(row.source_row_updated_at || row.UpdatedAt || '').trim(),\n        CreatedAt: String(row.CreatedAt || '').trim(),\n        UpdatedAt: String(row.UpdatedAt || '').trim()\n      },\n      pairedItem: { item: index }\n    };\n  });"
      }
    },
    {
      "id": "build-serper-query",
      "name": "Build URL Discovery Query",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1520,
        300
      ],
      "parameters": {
        "mode": "runOnceForEachItem",
        "jsCode": "const companyName = String($json.company_name || '').trim();\nconst cleanedName = companyName\n  .replace(/\\([^)]*\\)/g, ' ')\n  .replace(/[\"“”]/g, '')\n  .replace(/\\s+/g, ' ')\n  .trim();\nconst queryBase = cleanedName || companyName.replace(/[\"“”]/g, '').trim();\nfunction domainFromUrl(value) {\n  const raw = String(value || '').trim();\n  if (!raw) return '';\n  try {\n    const parsed = new URL(/^[a-z][a-z0-9+.-]*:\\/\\//i.test(raw) ? raw : 'https://' + raw);\n    return parsed.hostname.toLowerCase().replace(/^www\\./, '');\n  } catch {\n    return '';\n  }\n}\nfunction rediscoveryExcludedDomain(row) {\n  const carried = String(row.excluded_url_domain || '').trim().toLowerCase();\n  if (carried) return carried;\n  const reason = String(row.status_reason || '').trim();\n  return reason === 'skipped_url_validation_failed'\n    ? (String(row.canonical_domain || '').trim().toLowerCase() || domainFromUrl(row.url_picked || row.best_url || row.homepage_root_url))\n    : '';\n}\nconst excludedDomain = rediscoveryExcludedDomain($json);\nconst query = queryBase + ' Singapore' + (excludedDomain ? ' -site:' + excludedDomain : '');\nreturn {\n  json: {\n    ...$json,\n    serper_query: query,\n    excluded_url_domain: excludedDomain,\n    serper_body: {\n      q: query,\n      num: 10\n    }\n  }\n};"
      }
    },
    {
      "id": "search-serper-url-discovery",
      "name": "Search URL Discovery Serper",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1780,
        300
      ],
      "parameters": {
        "method": "POST",
        "url": "https://google.serper.dev/search",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "X-API-KEY",
              "value": "={{ $env.SERPER_API_KEY }}"
            },
            {
              "name": "Content-Type",
              "value": "application/json"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify($json.serper_body) }}",
        "options": {
          "timeout": 60000
        }
      },
      "continueOnFail": true
    },
    {
      "id": "prepare-serper-pick",
      "name": "Prepare URL Discovery Pick",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2040,
        300
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const rows = $('Build URL Discovery Query').all().map((item) => item.json || {});\nconst searchItems = $('Search URL Discovery Serper').all();\nconst groupedResults = new Map();\nconst groupedErrors = new Map();\nfunction sourceIndex(item, fallback) {\n  const paired = item.pairedItem;\n  if (Array.isArray(paired) && Number.isInteger(paired[0]?.item)) return paired[0].item;\n  if (paired && Number.isInteger(paired.item)) return paired.item;\n  return fallback;\n}\nfunction extractResults(payload) {\n  if (Array.isArray(payload)) return payload;\n  if (Array.isArray(payload?.body)) return payload.body;\n  if (Array.isArray(payload?.results)) return payload.results;\n  if (Array.isArray(payload?.organic)) return payload.organic;\n  if (Array.isArray(payload?.body?.organic)) return payload.body.organic;\n  if (Array.isArray(payload?.data)) return payload.data;\n  if (Array.isArray(payload?.items)) return payload.items;\n  if (payload?.title || payload?.url || payload?.link) return [payload];\n  return [];\n}\nfunction stringifyErrorPart(value) {\n  if (!value) return '';\n  if (typeof value === 'string') return value;\n  if (typeof value === 'number') return String(value);\n  if (typeof value === 'object') {\n    return String(value.message || value.error || value.code || value.statusCode || JSON.stringify(value));\n  }\n  return String(value);\n}\nfunction extractError(payload) {\n  const body = payload?.body && !Array.isArray(payload.body) ? payload.body : {};\n  const code = stringifyErrorPart(payload?.code || body.code || payload?.statusCode || body.statusCode);\n  const message = stringifyErrorPart(payload?.message || body.message || payload?.error || body.error || payload?.body);\n  if (!code && !message) return '';\n  return [code, message].filter(Boolean).join(': ');\n}\nfunction providerAccountError(value) {\n  const text = stringifyErrorPart(value).toLowerCase().replace(/[_-]+/g, ' ');\n  return /provider_account_error|insufficient balance|zero balance|balance not enough|no funds|out of funds|insufficient funds|quota|billing|payment required|invalid api key|invalid key|wrong api key|api key|unauthorized|too many requests|rate limit/.test(text);\n}\nsearchItems.forEach((item, fallbackIndex) => {\n  const index = sourceIndex(item, fallbackIndex);\n  const payload = item.json ?? {};\n  const existing = groupedResults.get(index) || [];\n  existing.push(...extractResults(payload));\n  groupedResults.set(index, existing);\n  const error = extractError(payload);\n  if (error && providerAccountError(error)) {\n    throw new Error('provider_account_error: Serper URL discovery failed: ' + error);\n  }\n  if (error) groupedErrors.set(index, error);\n});\nreturn rows.map((row, index) => {\n  const rawResults = groupedResults.get(index) || [];\n  const topResults = rawResults.slice(0, 10).map((result, resultIndex) => ({\n    rank: Number(result.rank || resultIndex + 1),\n    title: String(result.title || '').trim(),\n    url: String(result.url || result.link || '').trim(),\n    snippet: String(result.content || result.snippet || result.description || '').trim()\n  }));\n  const serperError = groupedErrors.get(index) || '';\n  const prompt = [\n    'Pick the official company website URL from the search results.',\n    'Company: ' + row.company_name,\n    'Return blank unless the result is clearly the company own website or official hosted page.',\n    'Rules:',\n    '- Prefer the official homepage/root domain when available.',\n    '- Prefer a normal company-owned domain over hosted landing pages when both are clearly official and usable; otherwise keep the clearly official hosted page.',\n    '- Accept close official-name variants when title/snippet confirms the same entity, e.g. added words like Services, Centre, Clinic, Caregivers, Medical, Group, Pte Ltd, or Singapore.',\n    '- Accept a hosted official site such as WordPress only when the title/snippet shows the company name plus Singapore contact/address/service evidence.',\n    '- For generic or short Singapore lead names, reject overseas .com results unless the title/snippet/domain shows Singapore evidence such as Singapore, SG, a local address, or a +65 phone.',\n    '- Reject directories, reviews, profiles, social media, messaging links, maps, job boards, news/articles, partner pages, stock pages, PDFs, and unrelated pages.',\n    '- Do not accept a third-party page just because the company name appears in the title, snippet, or URL path; the domain must carry the company identity unless it is a clearly official hosted page.',\n    '- Reject partial name matches where the result is about a different business/person, but do not reject just because the legal/site name has one extra descriptive word.',\n    '- Reject third-party pages where the company name appears only as a URL slug/path.',\n    row.excluded_url_domain ? '- Retry rule: reject the previous failed domain ' + row.excluded_url_domain + ' even if it appears official; choose another clearly official domain or return blank.' : '',\n    '- Do not guess. If none is clearly official, return an empty url.',\n    '- Return JSON only: {\"url\":\"\",\"reason\":\"\"}',\n    'Search results:',\n    JSON.stringify(topResults, null, 2)\n  ].join('\\n');\n  return {\n    json: {\n      Id: row.Id,\n      company_name: row.company_name,\n      serper_query: row.serper_query,\n      excluded_url_domain: String(row.excluded_url_domain || '').trim(),\n      serper_results: topResults,\n      serper_error: serperError,\n      status: String(row.status || '').trim(),\n      stage_mode: String(row.stage_mode || '').trim(),\n      run_id: String(row.run_id || '').trim(),\n      processing_started_at: String(row.processing_started_at || '').trim(),\n      processing_finished_at: String(row.processing_finished_at || '').trim(),\n      last_attempted_at: String(row.last_attempted_at || '').trim(),\n      attempt_count: String(row.attempt_count || '').trim() || '1',\n      status_reason: String(row.status_reason || '').trim(),\n      error_type: String(row.error_type || '').trim(),\n      error_message: String(row.error_message || '').trim(),\n      retry_eligible: String(row.retry_eligible || '').trim(),\n      source_row_created_at: String(row.source_row_created_at || row.CreatedAt || '').trim(),\n      source_row_updated_at: String(row.source_row_updated_at || row.UpdatedAt || '').trim(),\n      CreatedAt: String(row.CreatedAt || '').trim(),\n      UpdatedAt: String(row.UpdatedAt || '').trim(),\n      body: {\n        model: 'deepseek/deepseek-v4-flash',\n        temperature: 0,\n        messages: [\n          { role: 'system', content: 'You select official company homepage URLs from search results. Return strict JSON only.' },\n          { role: 'user', content: prompt }\n        ]\n      }\n    }\n  };\n});"
      }
    },
    {
      "id": "openrouter-url-pick",
      "name": "OpenRouter URL Pick",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        2300,
        300
      ],
      "parameters": {
        "method": "POST",
        "url": "https://openrouter.ai/api/v1/chat/completions",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "nodeCredentialType": "httpHeaderAuth",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify($json.body) }}",
        "options": {
          "timeout": 60000
        }
      },
      "credentials": {
        "httpHeaderAuth": {
          "id": "kVt6l9PuADkn7yhu",
          "name": "openrouter"
        }
      },
      "continueOnFail": true
    },
    {
      "id": "parse-url-pick",
      "name": "Parse URL Pick",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2560,
        300
      ],
      "parameters": {
        "mode": "runOnceForEachItem",
        "jsCode": "function stripFences(value) {\n  const fence = String.fromCharCode(96).repeat(3);\n  return String(value || '').replace(fence + 'json', '').replaceAll(fence, '').trim();\n}\nfunction normalizeUrl(value) {\n  const raw = String(value || '').trim();\n  if (!raw) return '';\n  const withProtocol = /^[a-z][a-z0-9+.-]*:\\/\\//i.test(raw) ? raw : 'https://' + raw;\n  const match = withProtocol.match(/^(https?:\\/\\/[^\\s?#]+)(?:[?#].*)?$/i);\n  return match ? match[1].replace(/\\/+$/, '/') : '';\n}\nfunction normalizeRootUrl(value) {\n  const normalized = normalizeUrl(value);\n  const match = normalized.match(/^(https?:\\/\\/[^\\/?#]+)(?:[\\/?#]|$)/i);\n  return match ? match[1] + '/' : '';\n}\nfunction canonicalDomain(value) {\n  const raw = String(value || '').trim();\n  if (!raw) return '';\n  const match = raw.match(/^(?:[a-z][a-z0-9+.-]*:\\/\\/)?([^\\s\\/?#]+)/i);\n  if (!match) return '';\n  let hostname = match[1].split('@').pop().split(':')[0];\n  hostname = hostname.toLowerCase().replace(/^www\\./, '');\n  const parts = hostname.split('.').filter(Boolean);\n  if (parts.length <= 2) return hostname;\n  const suffix2 = parts.slice(-2).join('.');\n  const sgSecondLevel = new Set(['com.sg', 'org.sg', 'net.sg', 'edu.sg', 'gov.sg', 'per.sg']);\n  if (sgSecondLevel.has(suffix2) && parts.length >= 3) return parts.slice(-3).join('.');\n  return parts.slice(-2).join('.');\n}\nfunction hostnameOf(value) {\n  const raw = String(value || '').trim();\n  const match = raw.match(/^(?:[a-z][a-z0-9+.-]*:\\/\\/)?([^\\s\\/?#]+)/i);\n  return match ? match[1].split('@').pop().split(':')[0].toLowerCase().replace(/^www\\./, '') : '';\n}\nfunction coreCompanyName(name) {\n  return String(name || '')\n    .replace(/\\b([a-z])\\.\\s*([a-z])\\.\\s*([a-z])\\.?\\b/gi, '$1$2$3')\n    .replace(/\\([^)]*\\)/g, ' ')\n    .replace(/\\s+@\\s+.*$/g, ' ')\n    .replace(/\\b(?:branch|outlet|jurong|tampines|orchard|novena|toa payoh|bukit merah|marina one)\\b/gi, ' ');\n}\nfunction tokenizeCompany(name) {\n  return coreCompanyName(name)\n    .toLowerCase()\n    .replace(/&/g, ' and ')\n    .split(/[^a-z0-9]+/)\n    .filter((token) => token.length >= 2);\n}\nfunction companyTokens(name) {\n  const stop = new Set(['singapore','clinic','group','health','healthcare','medical','dental','dentistry','surgery','family','centre','center','services','service','practice','practise','care','home','wellness','therapy','physio','physiotherapy','specialist','specialists','prevention','society','association','foundation','community','limited','pte','ltd','and','the']);\n  const distinctive = tokenizeCompany(name).filter((token) => !stop.has(token));\n  if (distinctive.length) return distinctive;\n  const brandStop = new Set(['singapore','limited','pte','ltd','and','the']);\n  return tokenizeCompany(name).filter((token) => !brandStop.has(token));\n}\nfunction hostIdentityScore(host, tokens) {\n  const hostText = String(host || '').toLowerCase().replace(/[^a-z0-9]/g, '');\n  return tokens.filter((token) => hostText.includes(String(token).replace(/[^a-z0-9]/g, ''))).length;\n}\nfunction strongHostIdentity(host, companyName) {\n  const tokens = companyTokens(companyName);\n  if (!tokens.length) return false;\n  const score = hostIdentityScore(host, tokens);\n  return tokens.length <= 1 ? score >= 1 : score >= Math.min(2, tokens.length);\n}\nfunction textHasCompanyIdentity(text, companyName) {\n  const tokens = companyTokens(companyName);\n  const normalized = String(text || '').toLowerCase();\n  if (!tokens.length) return false;\n  const hits = tokens.filter((token) => normalized.includes(token)).length;\n  return tokens.length <= 1 ? hits >= 1 : hits >= Math.min(2, tokens.length);\n}\nfunction companyAcronym(name) {\n  const parenAcronym = String(name || '').match(/\\(([A-Z0-9]{3,})\\)/);\n  if (parenAcronym) return parenAcronym[1].toLowerCase();\n  return String(name || '').toLowerCase().replace(/&/g, ' and ').split(/[^a-z0-9]+/).filter((token) => token && !['singapore','pte','ltd','limited'].includes(token)).map((token) => token[0]).join('');\n}\nfunction hostedLandingUrl(url) {\n  const host = hostnameOf(url);\n  const pathText = String(url || '').replace(/^https?:\\/\\/[^/]+/i, '').split(/[?#]/)[0].toLowerCase();\n  return /\\.(?:page|wordpress\\.com|wixsite\\.com|weebly\\.com|sites\\.google\\.com)$/i.test(host) || /\\/(?:hsg|landing|campaign|promo|signup|register)(?:\\/|$)/i.test(pathText);\n}\nfunction isBadCandidate(url) {\n  const host = hostnameOf(url);\n  const badHosts = ['facebook.com','instagram.com','linkedin.com','linktr.ee','tiktok.com','youtube.com','reddit.com','wikipedia.org','amazon.com','amazon.sg','lazada.sg','shopee.sg','qoo10.sg','carousell.sg','ebay.com','iherb.com','guardian.com.sg','watsons.com.sg','fairprice.com.sg','google.com','gmail.com','googlemail.com','mail.google.com','outlook.com','outlook.live.com','hotmail.com','live.com','yahoo.com','proton.me','protonmail.com','icloud.com','waze.com','yelp.com','whatclinic.com','practo.com','nlb.gov.sg','ltddir.com','threebestrated.sg','clinicgeek.com','getdoc.com','bharatopd.com','doc.sg','contact.page','bit.ly','tinyurl.com','wa.me','whatsapp.com','api.whatsapp.com','thebestsingapore.com','qanomed.com','skincataloq.com','wherecrowded.sg','healthcare.com.sg','healthcareasiamagazine.com','itiger.com','bizapore.com','sgpgrid.com','signalhire.com','apps.runtheworld.com','appsruntheworld.com','annualreports.com','bupaglobal.com','mom.gov.sg','mycareersfuture.gov.sg','glassdoor.com','indeed.com','jobstreet.com.sg','jobscentral.com.sg','giving.sg','charities.gov.sg','chas.sg','bebee.com','rocketreach.co','apollo.io','zoominfo.com','e27.co','guidesify.com','patsnap.com','tracxn.com','hotfrog.sg','infobel.sg','zipleaf.com','companies.sg','sgpbusiness.com','keepital.com','sgx.com','scam.sg','yellowpages.com.sg','streetdirectory.com','findhealthclinics.com','recordowl.com','doctorxdentist.com','workable.com','greenhouse.io','lever.co','ashbyhq.com','recruitee.com','teamtailor.com','smartrecruiters.com','breezy.hr','prospeo.io','craft.co','globaldatabase.com','arounddeal.com','businesstimes.com.sg','straitstimes.com','channelnewsasia.com'];\n  const pathText = String(url || '').replace(/^https?:\\/\\/[^/]+/i, '').split(/[?#]/)[0].toLowerCase();\n  const badPath = /(?:^|\\/|[-_])(provider|providers|feature|company|stock|item|profile|profiles|biz-review|asx|clinic-locator|directory|listing|careers?|jobs?|job-openings|organisation|organization|charity|glassdoor)(?:$|\\/|[-_])/i.test(pathText) || pathText.includes('/at/place/');\n  return !host || badHosts.some((bad) => host === bad || host.endsWith('.' + bad)) || badPath || /\\.(pdf|doc|docx|xls|xlsx)(?:$|[?#])/i.test(url);\n}\nfunction pathSegmentCount(url) {\n  return String(url || '').replace(/^https?:\\/\\/[^/]+/i, '').split(/[?#]/)[0].split('/').filter(Boolean).length;\n}\nfunction thirdPartyCompanySlug(url, companyName) {\n  const host = hostnameOf(url);\n  const tokens = companyTokens(companyName);\n  if (!host || tokens.length === 0 || strongHostIdentity(host, companyName)) return false;\n  const acronym = companyAcronym(companyName);\n  if (acronym.length >= 3 && host.replace(/[^a-z0-9]/g, '').includes(acronym)) return false;\n  const pathText = String(url || '').replace(/^https?:\\/\\/[^/]+/i, '').split(/[?#]/)[0].toLowerCase();\n  if (!pathText || pathText === '/') return false;\n  const pathWords = new Set(pathText.split(/[^a-z0-9]+/).filter(Boolean));\n  const pathHits = tokens.filter((token) => pathWords.has(token)).length;\n  if (pathHits >= Math.min(2, tokens.length)) return true;\n  return tokens.length === 1 && pathSegmentCount(url) > 1 && pathHits === 1;\n}\nfunction thirdPartyEditorialCandidate(url, companyName) {\n  const host = hostnameOf(url);\n  const tokens = companyTokens(companyName);\n  const acronym = companyAcronym(companyName);\n  const hostText = host.replace(/[^a-z0-9]/g, '');\n  const acronymIdentity = acronym.length >= 3 && hostText.includes(acronym);\n  if (!host || strongHostIdentity(host, companyName) || acronymIdentity) return false;\n  const pathText = String(url || '').replace(/^https?:\\/\\/[^/]+/i, '').split(/[?#]/)[0].toLowerCase();\n  const deepPath = pathSegmentCount(url) > 1;\n  const editorialHost = /(news|magazine|media|journal|review|times|today|insider|cosmeticsnews|businessreview)/i.test(host);\n  const editorialPath = /\\/(?:news|article|articles|blog|blogs|press|press-release|media|story|stories|magazine|insights|acquisition|mou|collaboration|partner|partners|launch)(?:\\/|$|-)/i.test(pathText);\n  const pathWords = new Set(pathText.split(/[^a-z0-9]+/).filter(Boolean));\n  const pathHits = tokens.filter((token) => pathWords.has(token)).length;\n  return deepPath && (editorialHost || editorialPath) && (pathHits > 0 || tokens.length <= 1);\n}\n\nfunction candidateUrls(result) {\n  const values = [];\n  const direct = normalizeUrl(result.url || result.link || '');\n  if (direct) values.push(direct);\n  const text = [result.snippet, result.content, result.description].map((value) => String(value || '')).join(' ');\n  for (const match of text.matchAll(/(?:https?:\\/\\/)?(?:www\\.)?[a-z0-9][a-z0-9-]*(?:\\.[a-z0-9-]+)*\\.[a-z]{2,}/gi)) {\n    const url = normalizeUrl(match[0]);\n    if (url) values.push(url);\n  }\n  return [...new Set(values)];\n}\n\nfunction hostLooksSingaporean(host) {\n  const value = String(host || '').toLowerCase();\n  return value.endsWith('.sg') || value.includes('singapore');\n}\nfunction resultText(result) {\n  return [result?.title, result?.snippet, result?.content, result?.description, result?.url, result?.link]\n    .map((value) => String(value || '').toLowerCase())\n    .join(' ');\n}\nfunction resultHasSingaporeEvidence(candidate, result) {\n  const host = hostnameOf(candidate);\n  if (hostLooksSingaporean(host)) return true;\n  const text = resultText(result);\n  const localWords = /\\bsingapore\\b|(?:^|[^a-z0-9])sg(?:[^a-z0-9]|$)|\\b(?:scotts|far east plaza|jalan|bukit merah|ang mo kio|toa payoh|novena|orchard|tampines|jurong|outram|marina one|tiong bahru|raffles|bedok|yishun|woodlands|sengkang|punggol|bishan|serangoon|clementi|hougang|queenstown|bukit timah|katong|kembangan|east coast|thomson|shenton|telok|seletar|changi)\\b|\\bblk\\s+\\d+|#\\d{2}-\\d{2,4}/i;\n  const localPhone = /(?:^|[^0-9])(?:\\+?65[\\s-]?)?[3689]\\d{3}[\\s-]?\\d{4}(?:[^0-9]|$)/;\n  return localWords.test(text) || localPhone.test(text);\n}\nfunction needsSingaporeEvidence(candidate, prepared) {\n  const host = hostnameOf(candidate);\n  if (!host || hostLooksSingaporean(host)) return false;\n  const tokens = companyTokens(prepared.company_name);\n  if (tokens.length <= 1) return true;\n  return hostIdentityScore(host, tokens) < Math.min(2, tokens.length);\n}\nfunction hasRequiredSingaporeEvidence(candidate, result, prepared) {\n  return !needsSingaporeEvidence(candidate, prepared) || resultHasSingaporeEvidence(candidate, result);\n}\n\nfunction excludedUrlDomain(prepared) {\n  return String(prepared.excluded_url_domain || '').trim().toLowerCase().replace(/^www\\./, '');\n}\nfunction isExcludedCandidate(url, prepared) {\n  const excluded = excludedUrlDomain(prepared);\n  return Boolean(excluded && canonicalDomain(url) === excluded);\n}\nfunction strongSearchIdentity(url, result, prepared) {\n  const host = hostnameOf(url);\n  if (!host || pathSegmentCount(url) > 1 || !hasRequiredSingaporeEvidence(url, result, prepared)) return false;\n  const tokens = companyTokens(prepared.company_name);\n  if (!tokens.length) return false;\n  const title = String(result.title || '').toLowerCase();\n  const text = [result.title, result.snippet].map((value) => String(value || '').toLowerCase()).join(' ');\n  const hostScore = hostIdentityScore(host, tokens);\n  const acronym = companyAcronym(prepared.company_name);\n  const hostText = host.replace(/[^a-z0-9]/g, '');\n  const acronymIdentity = acronym.length >= 3 && hostText.includes(acronym);\n  const titleIdentity = tokens.every((token) => title.includes(token) || host.includes(token));\n  const textIdentity = textHasCompanyIdentity(text, prepared.company_name);\n  const rank = Number(result.rank || 99);\n  if (rank > 3) return false;\n  return (titleIdentity || (textIdentity && hostScore >= 1)) && (hostScore >= 1 || acronymIdentity || hostLooksSingaporean(host));\n}\nfunction deterministicOfficialFallback(prepared) {\n  const tokens = companyTokens(prepared.company_name);\n  const acronym = companyAcronym(prepared.company_name);\n  for (const result of (prepared.serper_results || [])) {\n    const urls = candidateUrls(result);\n    const title = String(result.title || '').toLowerCase();\n    const text = [result.title, result.snippet].map((value) => String(value || '').toLowerCase()).join(' ');\n    const officialText = /\\b(?:official\\s+)?(?:website|site|homepage)\\b|\\bhomepage\\b|\\bour\\s+clinic\\b|get directions|phone:|address|contact us/.test(text);\n    const textIdentity = textHasCompanyIdentity(text, prepared.company_name);\n    for (const candidate of urls) {\n      if (!candidate || isExcludedCandidate(candidate, prepared) || isBadCandidate(candidate) || thirdPartyCompanySlug(candidate, prepared.company_name) || thirdPartyEditorialCandidate(candidate, prepared.company_name) || !hasRequiredSingaporeEvidence(candidate, result, prepared)) continue;\n      const host = hostnameOf(candidate);\n      const hostStrong = strongHostIdentity(host, prepared.company_name);\n      const acronymIdentity = acronym.length >= 3 && host.replace(/[^a-z0-9]/g, '').includes(acronym);\n      const hits = tokens.filter((token) => host.includes(token) || text.includes(token)).length;\n      const requiredHits = tokens.length >= 2 ? Math.min(2, tokens.length) : 1;\n      const titleIdentity = tokens.length > 0 && tokens.every((token) => title.includes(token) || host.includes(token));\n      const deepPath = pathSegmentCount(candidate) > 1;\n      const hostedOfficial = hostedLandingUrl(candidate) && textIdentity && officialText;\n      if (strongSearchIdentity(candidate, result, prepared)) return candidate;\n      if (!hostStrong && !acronymIdentity && !hostedOfficial) continue;\n      if (deepPath && !hostStrong && !hostedOfficial) continue;\n      if (hostStrong && (hits >= requiredHits || titleIdentity || textIdentity || officialText)) return candidate;\n      if (acronymIdentity && (officialText || textIdentity)) return candidate;\n      if (hostedOfficial) return candidate;\n    }\n  }\n  return '';\n}\nfunction preferNormalOfficialUrl(pickedUrl, prepared) {\n  if (!pickedUrl || !hostedLandingUrl(pickedUrl)) return pickedUrl;\n  const tokens = companyTokens(prepared.company_name);\n  const pickedCanonical = canonicalDomain(pickedUrl);\n  for (const result of (prepared.serper_results || [])) {\n    const text = [result.title, result.snippet].map((value) => String(value || '').toLowerCase()).join(' ');\n    const textIdentity = textHasCompanyIdentity(text, prepared.company_name);\n    const hits = tokens.filter((token) => text.includes(token)).length;\n    const requiredHits = tokens.length >= 2 ? Math.min(2, tokens.length) : 1;\n    if (!textIdentity && hits < requiredHits) continue;\n    for (const candidate of candidateUrls(result)) {\n      if (!candidate || isBadCandidate(candidate) || hostedLandingUrl(candidate) || thirdPartyCompanySlug(candidate, prepared.company_name) || thirdPartyEditorialCandidate(candidate, prepared.company_name) || !hasRequiredSingaporeEvidence(candidate, result, prepared)) continue;\n      const candidateCanonical = canonicalDomain(candidate);\n      if (!candidateCanonical || candidateCanonical === pickedCanonical) continue;\n      const host = hostnameOf(candidate);\n      if (strongHostIdentity(host, prepared.company_name)) return normalizeRootUrl(candidate) || candidate;\n    }\n  }\n  return pickedUrl;\n}\nfunction llmPickTrusted(url, prepared) {\n  if (!url || isExcludedCandidate(url, prepared) || isBadCandidate(url) || thirdPartyCompanySlug(url, prepared.company_name) || thirdPartyEditorialCandidate(url, prepared.company_name)) return false;\n  const canonical = canonicalDomain(url);\n  const tokens = companyTokens(prepared.company_name);\n  const acronym = companyAcronym(prepared.company_name);\n  for (const result of (prepared.serper_results || [])) {\n    const urls = candidateUrls(result);\n    if (!urls.some((candidate) => canonicalDomain(candidate) === canonical)) continue;\n    const title = String(result.title || '').toLowerCase();\n    const text = [result.title, result.snippet].map((value) => String(value || '').toLowerCase()).join(' ');\n    if (!hasRequiredSingaporeEvidence(url, result, prepared)) return false;\n    const host = hostnameOf(url);\n    const hostStrong = strongHostIdentity(host, prepared.company_name);\n    const acronymIdentity = acronym.length >= 3 && host.replace(/[^a-z0-9]/g, '').includes(acronym);\n    const deepPath = pathSegmentCount(url) > 1;\n    const hits = tokens.filter((token) => host.includes(token) || text.includes(token)).length;\n    const requiredHits = tokens.length >= 2 ? Math.min(2, tokens.length) : 1;\n    const officialText = /\\b(?:official\\s+)?(?:website|site|homepage)\\b|\\bhomepage\\b|\\bour\\s+clinic\\b|get directions|phone:|address|contact us/.test(text);\n    const titleIdentity = tokens.length > 0 && tokens.every((token) => title.includes(token) || host.includes(token));\n    const textIdentity = textHasCompanyIdentity(text, prepared.company_name);\n    const hostedOfficial = hostedLandingUrl(url) && textIdentity && officialText;\n    if (strongSearchIdentity(url, result, prepared)) return true;\n    if (!hostStrong && !acronymIdentity && !hostedOfficial) return false;\n    if (deepPath && !hostStrong && !hostedOfficial) return false;\n    if (hostStrong && (hits >= requiredHits || titleIdentity || textIdentity || officialText)) return true;\n    if (acronymIdentity && (officialText || textIdentity)) return true;\n    if (hostedOfficial) return true;\n    return false;\n  }\n  return false;\n}\nconst prepared = $('Prepare URL Discovery Pick').all()[$itemIndex]?.json || {};\nconst stageMode = String(prepared.stage_mode || '').trim();\nconst response = $json || {};\nfunction providerErrorText(value) {\n  if (!value) return '';\n  if (typeof value === 'string') return value;\n  if (typeof value === 'number') return String(value);\n  if (typeof value === 'object') return String(value.message || value.error?.message || value.error || value.code || value.statusCode || value.body?.message || value.body?.error || JSON.stringify(value));\n  return String(value);\n}\nfunction providerAccountError(value) {\n  const text = providerErrorText(value).toLowerCase().replace(/[_-]+/g, ' ');\n  return /provider_account_error|insufficient balance|zero balance|balance not enough|no funds|out of funds|insufficient funds|quota|billing|payment required|invalid api key|invalid key|wrong api key|api key|unauthorized|too many requests|rate limit/.test(text);\n}\nconst providerFailure = response.error || response.message || response.code || response.statusCode || response.body?.error || response.body?.message;\nif (providerFailure && providerAccountError(providerFailure)) {\n  throw new Error('provider_account_error: OpenRouter URL picker failed: ' + providerErrorText(providerFailure));\n}\nconst content = String(response.choices?.[0]?.message?.content || response.body?.choices?.[0]?.message?.content || '').trim();\nlet parsed = {};\ntry { parsed = JSON.parse(stripFences(content)); } catch {}\nconst searchFailed = Boolean(prepared.serper_error) && (!Array.isArray(prepared.serper_results) || prepared.serper_results.length === 0);\nconst rawLlmPickedUrl = searchFailed ? '' : normalizeUrl(parsed.url || '');\nconst llmPickedUrl = llmPickTrusted(rawLlmPickedUrl, prepared) ? rawLlmPickedUrl : '';\nconst fallbackPickedUrl = (!searchFailed && !llmPickedUrl) ? deterministicOfficialFallback(prepared) : '';\nconst candidatePickedUrl = preferNormalOfficialUrl(llmPickedUrl || fallbackPickedUrl, prepared);\nconst pickedUrl = isExcludedCandidate(candidatePickedUrl, prepared) ? '' : candidatePickedUrl;\nconst operatingRootUrl = normalizeRootUrl(pickedUrl);\nconst canonical = canonicalDomain(operatingRootUrl || pickedUrl);\nconst finishedAt = pickedUrl ? String(prepared.processing_finished_at || '') : new Date().toISOString();\nconst noUrlMessage = 'No official URL selected from first 10 Serper results';\nconst errorMessage = searchFailed ? ('Serper URL discovery failed: ' + prepared.serper_error) : (pickedUrl ? '' : noUrlMessage);\nconst statusReason = searchFailed ? 'search_failed' : (pickedUrl ? 'url_picked' : 'no_official_url_found');\nconst reason = searchFailed ? 'Serper URL discovery failed before candidate selection.' : (rawLlmPickedUrl && !llmPickedUrl ? 'LLM pick rejected by deterministic identity check.' : (fallbackPickedUrl ? 'Deterministic official fallback selected from Serper result.' : String(parsed.reason || '').trim()));\nconst evidence = {\n  query: prepared.serper_query,\n  picked_url: pickedUrl,\n  operating_root_url: operatingRootUrl,\n  canonical_domain: canonical,\n  search_error: prepared.serper_error || '',\n  excluded_url_domain: excludedUrlDomain(prepared),\n  reason,\n  llm_picked_url: rawLlmPickedUrl,\n  fallback_url: fallbackPickedUrl,\n  normalized_from_hosted_url: (llmPickedUrl || fallbackPickedUrl) !== pickedUrl ? (llmPickedUrl || fallbackPickedUrl) : '',\n  first_10_results: prepared.serper_results || [],\n  llm_raw: content\n};\nreturn {\n  json: {\n    Id: prepared.Id,\n    company_name: prepared.company_name,\n    stage_mode: stageMode,\n    url_picked: pickedUrl,\n    homepage_root_url: operatingRootUrl,\n    best_url: operatingRootUrl || pickedUrl,\n    canonical_domain: canonical,\n    duplicate_of_id: null,\n    search_evidence_json: JSON.stringify(evidence),\n    status: searchFailed ? 'failed' : (pickedUrl ? 'url_picked' : 'skipped'),\n    last_stage: searchFailed ? 'url_discovery' : 'url_pick',\n    last_error: errorMessage,\n    status_reason: statusReason,\n    run_id: String(prepared.run_id || ''),\n    processing_started_at: String(prepared.processing_started_at || ''),\n    processing_finished_at: finishedAt,\n    last_attempted_at: String(prepared.last_attempted_at || ''),\n    attempt_count: String(prepared.attempt_count || '').trim() || '1',\n    error_type: searchFailed ? 'search_error' : (pickedUrl ? '' : 'no_official_url'),\n    error_message: errorMessage,\n    retry_eligible: searchFailed ? 'true' : 'false',\n    source_row_created_at: String(prepared.source_row_created_at || prepared.CreatedAt || ''),\n    source_row_updated_at: String(prepared.source_row_updated_at || prepared.UpdatedAt || '')\n  }\n};"
      }
    },
    {
      "id": "patch-url-picked",
      "name": "Patch URL Picked",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        3340,
        300
      ],
      "parameters": {
        "method": "PATCH",
        "url": "https://nocodb-production-f802.up.railway.app/api/v1/db/data/bulk/noco/pb7f1zou786xyqc/mey3zgihq7o4at9",
        "sendHeaders": false,
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify([{ Id: $json.Id, url_picked: $json.url_picked, homepage_root_url: $json.homepage_root_url, best_url: $json.best_url, canonical_domain: $json.canonical_domain, duplicate_of_id: $json.duplicate_of_id, search_evidence_json: $json.search_evidence_json, status: $json.status, last_stage: $json.last_stage, last_error: $json.last_error, status_reason: $json.status_reason, run_id: $json.run_id, processing_started_at: $json.processing_started_at, processing_finished_at: $json.processing_finished_at, last_attempted_at: $json.last_attempted_at, attempt_count: $json.attempt_count, error_type: $json.error_type, error_message: $json.error_message, retry_eligible: $json.retry_eligible, source_row_created_at: $json.source_row_created_at, source_row_updated_at: $json.source_row_updated_at, evidence_url: '', scrape_url: '', website_content: '', website_scrape: '', operating_company_root_name: '', company_homepage_name: '', parent_company: '', confidence: '', notes: '', source_urls: '', fallback_used: '', evidence_gap: '' }]) }}",
        "options": {
          "timeout": 30000
        },
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "nocoDbApiToken"
      },
      "credentials": {
        "nocoDbApiToken": {
          "id": "d5Rz1LXeEoHMrZwo",
          "name": "NocoDB Token account"
        }
      },
      "retryOnFail": true,
      "maxTries": 2,
      "waitBetweenTries": 1000
    },
    {
      "id": "find-canonical-duplicate",
      "name": "Find Canonical Duplicate",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        2820,
        300
      ],
      "parameters": {
        "method": "GET",
        "url": "={{ $json.canonical_domain ? 'https://nocodb-production-f802.up.railway.app/api/v1/db/data/noco/pb7f1zou786xyqc/mey3zgihq7o4at9?where=' + encodeURIComponent('(canonical_domain,eq,' + $json.canonical_domain + ')~and(Id,neq,' + $json.Id + ')') + '&fields=Id,company_name,canonical_domain,url_picked,status&limit=1' : 'https://nocodb-production-f802.up.railway.app/api/v1/db/data/noco/pb7f1zou786xyqc/mey3zgihq7o4at9?where=(Id,eq,-1)&fields=Id&limit=1' }}",
        "sendHeaders": false,
        "options": {
          "timeout": 30000
        },
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "nocoDbApiToken"
      },
      "credentials": {
        "nocoDbApiToken": {
          "id": "d5Rz1LXeEoHMrZwo",
          "name": "NocoDB Token account"
        }
      },
      "continueOnFail": true
    },
    {
      "id": "apply-dedupe-result",
      "name": "Apply Dedupe Result",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        3080,
        300
      ],
      "parameters": {
        "mode": "runOnceForEachItem",
        "jsCode": "const picked = $('Parse URL Pick').all()[$itemIndex]?.json || {};\nconst lookup = $json || {};\nconst dbMatches = Array.isArray(lookup.list) ? lookup.list : [];\nconst parsedItems = $('Parse URL Pick').all().map((item) => item.json || {});\nconst earlierBatchDuplicate = parsedItems\n  .filter((item) => item.canonical_domain && item.canonical_domain === picked.canonical_domain)\n  .filter((item) => Number(item.Id) < Number(picked.Id))\n  .sort((a, b) => Number(a.Id) - Number(b.Id))[0];\nconst dbDuplicate = dbMatches\n  .filter((row) => Number(row.Id) < Number(picked.Id))\n  .sort((a, b) => Number(a.Id) - Number(b.Id))[0];\nconst duplicate = dbDuplicate || earlierBatchDuplicate || null;\nif (picked.canonical_domain && duplicate) {\n  const errorMessage = 'Duplicate canonical_domain ' + picked.canonical_domain + ' of row ' + duplicate.Id;\n  return {\n    json: {\n      ...picked,\n      duplicate_of_id: Number(duplicate.Id),\n      status: 'skipped',\n      last_stage: 'dedupe',\n      last_error: errorMessage,\n      status_reason: 'duplicate_canonical_domain',\n      processing_finished_at: new Date().toISOString(),\n      error_type: 'duplicate',\n      error_message: errorMessage,\n      retry_eligible: 'false'\n    }\n  };\n}\nreturn { json: picked };"
      }
    },
    {
      "id": "prepare-public-enrichment",
      "name": "Prepare Public Enrichment",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1520,
        520
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "return $input.all().flatMap((item) => {\n  const row = item.json || {};\n  if (row.status !== 'processing') {\n    return [];\n  }\n  const matchedUrl = String(row.url_picked || '').trim();\n  const manualUrl = String(row.manual_url_override || '').trim();\n  const urlPicked = String(row.homepage_root_url || row.best_url || row.url_picked || manualUrl).trim();\n  if (!urlPicked || row.duplicate_of_id) {\n    return [];\n  }\n  const statusReason = [row.status_reason, row.error_type].filter(Boolean).join(' ').trim().toLowerCase();\n  const explicitStage = String(row.enrichment_stage || row.public_enrichment_stage || '').trim();\n  const attemptCount = Number(String(row.attempt_count || '').trim() || '1');\n  const timeoutRetry = statusReason.includes('enrichment_timeout') || statusReason.includes('enrichment_transport_timeout');\n  const retryEscalates = attemptCount > 1 && !timeoutRetry && !statusReason.includes('processing:crawl');\n  const shouldDeepRetry = explicitStage === 'deep_retry' || statusReason.includes('weak_retry') || statusReason.includes('thin_content') || statusReason.includes('no_services_detected') || statusReason.includes('no_locations_detected') || statusReason.includes('no_team_or_contact_page') || statusReason.includes('challenge_detected') || retryEscalates;\n  const stage = shouldDeepRetry ? 'deep_retry' : 'fast';\n  return [{\n    json: {\n      Id: row.Id,\n      company_name: String(row.company_name || '').trim(),\n      url_picked: urlPicked,\n      matched_url: matchedUrl,\n      enrichment_stage: stage,\n      page_limit: stage === 'deep_retry' ? 5 : 3,\n      page_timeout_ms: stage === 'deep_retry' ? 15000 : 12000,\n      request_delay_seconds: stage === 'deep_retry' ? 0.5 : 0.15,\n      scrape_char_limit: stage === 'deep_retry' ? 160000 : 120000,\n      per_row_page_concurrency: stage === 'deep_retry' ? 2 : 2,\n      row_timeout_seconds: stage === 'deep_retry' ? 120 : 75,\n      allow_low_limits: false,\n      manual_url_override: manualUrl,\n      allow_cross_domain_redirect: Boolean(manualUrl) || row.allow_cross_domain_redirect === true || String(row.allow_cross_domain_redirect || '').trim().toLowerCase() === 'true',\n      run_id: String(row.run_id || ''),\n      processing_started_at: String(row.processing_started_at || ''),\n      last_attempted_at: String(row.last_attempted_at || ''),\n      attempt_count: String(row.attempt_count || '').trim() || '1',\n      source_row_created_at: String(row.source_row_created_at || row.CreatedAt || ''),\n      source_row_updated_at: String(row.source_row_updated_at || row.UpdatedAt || '')\n    },\n    pairedItem: item.pairedItem\n  }];\n});"
      }
    },
    {
      "id": "crawl4ai-public-enrich",
      "name": "Crawl4AI Public Enrich",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1780,
        520
      ],
      "parameters": {
        "method": "POST",
        "url": "={{ (() => { const base = String($env.CRAWL4AI_SCRAPER_URL || 'https://n8n-rayn-workflows-production.up.railway.app/scrape'); const stage = String($json.enrichment_stage || 'fast'); const httpFirstEnabled = !['false', '0', 'no'].includes(String($env.RAYN_PUBLIC_ENRICH_HTTP_FIRST || 'true').toLowerCase()); const manualOverride = $json.allow_cross_domain_redirect === true || String($json.allow_cross_domain_redirect || '').trim().toLowerCase() === 'true'; const path = manualOverride ? '/public-enrich' : (httpFirstEnabled && stage !== 'deep_retry' ? '/public-enrich-http-first' : '/public-enrich'); return base.replace(/\\/(scrape|public-enrich|public-enrich-http-first)$/, path); })() }}",
        "sendHeaders": false,
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify($json) }}",
        "options": {
          "timeout": 420000
        }
      },
      "continueOnFail": true
    },
    {
      "id": "prepare-enrichment-patch",
      "name": "Prepare Enrichment Patch",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2040,
        520
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const sourceItems = $('Prepare Public Enrichment').all();\nfunction stringify(value) {\n  if (!value) return '';\n  if (typeof value === 'string') return value;\n  if (typeof value === 'number') return String(value);\n  if (typeof value === 'object') {\n    return String(value.message || value.error || value.code || JSON.stringify(value));\n  }\n  return String(value);\n}\nfunction providerAccountError(value) {\n  const text = stringify(value).toLowerCase().replace(/[_-]+/g, ' ');\n  return /provider_account_error|insufficient balance|zero balance|balance not enough|no funds|out of funds|insufficient funds|quota|billing|payment required|invalid api key|invalid key|wrong api key|api key|unauthorized|too many requests|rate limit/.test(text);\n}\nfunction providerErrorFields(payload) {\n  const patch = payload && typeof payload.patch === 'object' ? payload.patch : {};\n  return [payload?.error, payload?.message, payload?.code, payload?.statusCode, payload?.body?.error, payload?.body?.message, patch.last_error, patch.error_message].filter(Boolean).join(' | ');\n}\nfunction isTransportTimeout(value) {\n  const text = stringify(value).toLowerCase().replace(/[_-]+/g, ' ');\n  return text.includes('timeout') || text.includes('etimedout') || text.includes('socket hang up') || text.includes('econnreset') || text.includes('aborted') || text.includes('bad gateway') || text.includes('service unavailable') || text.includes('gateway timeout') || /\\b(502|503|504)\\b/.test(text);\n}\nconst NOCO_LONG_TEXT_LIMIT = 95000;\nfunction limitLongText(value, max = NOCO_LONG_TEXT_LIMIT) {\n  const text = stringify(value);\n  if (text.length <= max) return text;\n  return text.slice(0, max - 96) + '\\n[truncated_for_nocodb_longtext original_length=' + text.length + ']';\n}\nfunction compactStructuredData(value, max = NOCO_LONG_TEXT_LIMIT) {\n  const text = stringify(value);\n  if (text.length <= max) return text;\n  let parsed = null;\n  try { parsed = typeof value === 'string' ? JSON.parse(value) : value; } catch {}\n  const compact = parsed && typeof parsed === 'object' && !Array.isArray(parsed) ? {\n    has_json_ld: Boolean(parsed.has_json_ld),\n    schema_types: Array.isArray(parsed.schema_types) ? parsed.schema_types.slice(0, 20) : [],\n    schema_names: Array.isArray(parsed.schema_names) ? parsed.schema_names.slice(0, 20) : [],\n    og_site_name: String(parsed.og_site_name || ''),\n    sitemap_urls: Array.isArray(parsed.sitemap_urls) ? parsed.sitemap_urls.slice(0, 20) : [],\n    truncated_for_nocodb_longtext: true,\n    original_length: text.length\n  } : {\n    truncated_for_nocodb_longtext: true,\n    original_length: text.length,\n    preview: text.slice(0, 4000)\n  };\n  const compactText = JSON.stringify(compact);\n  return compactText.length <= max ? compactText : JSON.stringify({ truncated_for_nocodb_longtext: true, original_length: text.length });\n}\nfunction capNocoLongTextFields(patch) {\n  patch.structured_data_detected = compactStructuredData(patch.structured_data_detected || '');\n  patch.website_content = limitLongText(patch.website_content || '');\n  patch.website_scrape = limitLongText(patch.website_scrape || '');\n  patch.services_detected = limitLongText(patch.services_detected || '', 20000);\n  patch.locations_detected = limitLongText(patch.locations_detected || '', 20000);\n  patch.contact_info_detected = limitLongText(patch.contact_info_detected || '', 20000);\n  patch.leadership_or_team_signals = limitLongText(patch.leadership_or_team_signals || '', 20000);\n  patch.notes = limitLongText(patch.notes || '', 4000);\n  patch.last_error = limitLongText(patch.last_error || '', 4000);\n  return patch;\n}\nfunction enrichmentDepth(patch) {\n  try {\n    const parsed = typeof patch.structured_data_detected === 'string' ? JSON.parse(patch.structured_data_detected) : patch.structured_data_detected;\n    return (parsed && parsed.enrichment_depth) || {};\n  } catch {\n    return {};\n  }\n}\nfunction terminalStatus(patch) {\n  const depthMeta = enrichmentDepth(patch);\n  const depth = String(patch.enrichment_depth_status || depthMeta.enrichment_depth_status || '').trim();\n  const stage = String(patch.last_stage || patch.crawl_status || '').trim();\n  if (stage === 'crawled') return 'completed';\n  if (stage === 'partial') return String(patch.best_url || '').trim() ? 'completed' : 'needs_review';\n  if (stage === 'crawl_failed' || stage === 'enrichment_error') return isTransportTimeout(patch.last_error || patch.error_message || '') ? 'failed_retryable' : 'failed';\n  if (stage === 'blocked_by_robots' || stage.startsWith('skipped_')) return 'skipped';\n  if (!String(patch.best_url || '').trim()) return 'needs_review';\n  return 'completed';\n}\nfunction statusReason(status, patch) {\n  const stage = String(patch.last_stage || patch.crawl_status || '').trim();\n  const depthMeta = enrichmentDepth(patch);\n  const depth = String(patch.enrichment_depth_status || depthMeta.enrichment_depth_status || '').trim();\n  const weakReason = String(patch.weak_enrichment_reason || depthMeta.weak_enrichment_reason || '').trim();\n  if (status === 'completed') {\n    if (weakReason) return `enrichment_completed_${weakReason}`;\n    return stage === 'partial' ? 'enrichment_completed_with_subpage_warnings' : 'enrichment_completed';\n  }\n  if (status === 'needs_review') return stage === 'partial' ? 'partial_crawl' : 'ambiguous_enrichment';\n  if (status === 'skipped') return stage || 'skipped';\n  if (status === 'failed_retryable' && isTransportTimeout(patch.last_error || patch.error_message || '')) return 'enrichment_timeout';\n  return stage || 'failed';\n}\nreturn $input.all().flatMap((item, index) => {\n  const source = sourceItems[index]?.json || {};\n  const payload = (item.json?.body && typeof item.json.body === 'object' && !Array.isArray(item.json.body)) ? item.json.body : (item.json || {});\n  const providerErrorText = providerErrorFields(payload);\n  if (providerAccountError(providerErrorText)) {\n    throw new Error('provider_account_error: Crawl4AI public enrichment failed: ' + providerErrorText);\n  }\n  const finishedAt = new Date().toISOString();\n  const sourceAttemptCount = Number(String(source.attempt_count || '').trim() || '1');\n  const maxAttempts = Math.max(1, Number($env.RAYN_MAX_ENRICHMENT_ATTEMPTS || 4));\n  if (payload && payload.patch && payload.patch.Id) {\n    const patch = { ...payload.patch };\n    const finalStatus = terminalStatus(patch);\n    const lastError = stringify(patch.last_error || '');\n    if (finalStatus === 'failed_retryable' && isTransportTimeout(lastError)) {\n      const sourceBestUrl = String(source.best_url || source.url_picked || source.homepage_root_url || patch.best_url || patch.homepage_root_url || '').trim();\n      const maxAttemptsReached = sourceAttemptCount >= maxAttempts;\n      const timeoutPatch = {\n        ...patch,\n        Id: source.Id,\n        status: maxAttemptsReached ? 'skipped' : 'failed_retryable',\n        status_reason: maxAttemptsReached ? 'skipped_enrichment_timeout_final' : 'enrichment_transport_timeout_retry_required',\n        best_url: sourceBestUrl || String(patch.best_url || '').trim(),\n        homepage_root_url: sourceBestUrl || String(patch.homepage_root_url || '').trim(),\n        last_error: lastError,\n        notes: lastError,\n        run_id: String(source.run_id || ''),\n        processing_started_at: String(source.processing_started_at || ''),\n        processing_finished_at: finishedAt,\n        last_attempted_at: String(source.last_attempted_at || ''),\n        attempt_count: String(source.attempt_count || '').trim() || '1',\n        error_type: maxAttemptsReached ? 'enrichment_timeout_final' : 'enrichment_transport_timeout',\n        error_message: maxAttemptsReached ? (lastError || 'Crawl enrichment timed out after final attempt; row skipped before outreach.') : (lastError || 'Crawl enrichment timed out; retry required before outreach can be sent.'),\n        retry_eligible: maxAttemptsReached ? 'false' : 'true',\n        contact_search_status: '',\n        contact_search_reason: '',\n        email_send_ready: false,\n        final_send_gate_passed: false,\n        automation_decision: '',\n        automation_decision_reason: '',\n        automation_blockers_json: maxAttemptsReached ? '[\"skipped_enrichment_timeout_final\"]' : '[\"enrichment_transport_timeout_retry_required\"]',\n        human_review_status: 'not_ready',\n        send_status: '',\n        sequence_status: '',\n        instantly_sync_status: 'not_synced',\n        email_1_subject: '',\n        email_1_body: '',\n        email_1_llm_rewritten: false,\n        email_2_subject: '',\n        email_2_body: '',\n        email_2_llm_rewritten: false,\n        email_3_subject: '',\n        email_3_body: '',\n        email_quality_flags: '[]',\n        severe_email_flags: '[]',\n        source_row_created_at: String(source.source_row_created_at || ''),\n        source_row_updated_at: String(source.source_row_updated_at || '')\n      };\n      capNocoLongTextFields(timeoutPatch);\n      return [{ json: timeoutPatch, pairedItem: item.pairedItem }];\n    }\n    const maxAttemptsReached = finalStatus === 'failed_retryable' && sourceAttemptCount >= maxAttempts;\n    const resolvedStatus = maxAttemptsReached ? 'failed' : finalStatus;\n    patch.status = resolvedStatus;\n    patch.run_id = String(source.run_id || '');\n    patch.processing_started_at = String(source.processing_started_at || '');\n    patch.processing_finished_at = finishedAt;\n    patch.last_attempted_at = String(source.last_attempted_at || '');\n    patch.attempt_count = String(source.attempt_count || '').trim() || '1';\n    patch.status_reason = maxAttemptsReached ? 'enrichment_timeout_max_attempts' : statusReason(resolvedStatus, patch);\n    patch.error_type = resolvedStatus === 'failed' || resolvedStatus === 'failed_retryable' ? (isTransportTimeout(lastError) ? 'enrichment_timeout' : (String(patch.last_stage || patch.crawl_status || '').trim() || 'enrichment_error')) : '';\n    patch.error_message = resolvedStatus === 'failed' || resolvedStatus === 'failed_retryable' ? lastError : '';\n    const depthMeta = enrichmentDepth(patch);\n    patch.retry_eligible = maxAttemptsReached ? 'false' : (resolvedStatus === 'failed' || resolvedStatus === 'failed_retryable' || String(patch.enrichment_depth_status || depthMeta.enrichment_depth_status || '') === 'weak_retry_needed' ? 'true' : 'false');\n    if (finalStatus === 'completed' && !String(source.contact_search_status || '').trim()) {\n      patch.contact_search_status = 'pending';\n      patch.contact_search_reason = 'pending_after_enrichment';\n    }\n    patch.source_row_created_at = String(source.source_row_created_at || '');\n    patch.source_row_updated_at = String(source.source_row_updated_at || '');\n    capNocoLongTextFields(patch);\n    return [{ json: patch, pairedItem: item.pairedItem }];\n  }\n  const errorText = stringify(payload.error || payload.message || payload.code || 'Public enrichment request failed');\n  const genericMaxAttemptsReached = sourceAttemptCount >= maxAttempts;\n  if (isTransportTimeout(errorText)) {\n    const sourceBestUrl = String(source.best_url || source.url_picked || source.homepage_root_url || '').trim();\n    const genericMaxAttemptsReached = sourceAttemptCount >= maxAttempts;\n    return [{\n      json: {\n        Id: source.Id,\n        status: genericMaxAttemptsReached ? 'failed' : 'failed_retryable',\n        best_url: sourceBestUrl,\n        homepage_root_url: sourceBestUrl,\n        last_stage: 'enrichment_timeout',\n        last_error: errorText,\n        notes: errorText,\n        run_id: String(source.run_id || ''),\n        processing_started_at: String(source.processing_started_at || ''),\n        processing_finished_at: finishedAt,\n        last_attempted_at: String(source.last_attempted_at || ''),\n        attempt_count: String(source.attempt_count || '').trim() || '1',\n        status_reason: genericMaxAttemptsReached ? 'enrichment_timeout_max_attempts' : 'enrichment_transport_timeout_retry_required',\n        error_type: 'enrichment_transport_timeout',\n        error_message: errorText,\n        retry_eligible: genericMaxAttemptsReached ? 'false' : 'true',\n        contact_search_status: '',\n        contact_search_reason: '',\n        email_send_ready: false,\n        final_send_gate_passed: false,\n        automation_decision: '',\n        automation_decision_reason: '',\n        automation_blockers_json: '[\"enrichment_transport_timeout_retry_required\"]',\n        human_review_status: 'not_ready',\n        send_status: '',\n        sequence_status: '',\n        instantly_sync_status: 'not_synced',\n        email_1_subject: '',\n        email_1_body: '',\n        email_1_llm_rewritten: false,\n        email_2_subject: '',\n        email_2_body: '',\n        email_2_llm_rewritten: false,\n        email_3_subject: '',\n        email_3_body: '',\n        email_quality_flags: '[]',\n        severe_email_flags: '[]',\n        source_row_created_at: String(source.source_row_created_at || ''),\n        source_row_updated_at: String(source.source_row_updated_at || '')\n      },\n      pairedItem: item.pairedItem\n    }];\n  }\n  if (false) {\n    return [{\n      json: {\n        Id: source.Id,\n        status: 'failed_retryable',\n        last_stage: 'enrichment_timeout',\n        last_error: errorText,\n        notes: errorText,\n        run_id: String(source.run_id || ''),\n        processing_started_at: String(source.processing_started_at || ''),\n        processing_finished_at: finishedAt,\n        last_attempted_at: String(source.last_attempted_at || ''),\n        attempt_count: String(source.attempt_count || '').trim() || '1',\n        status_reason: 'enrichment_transport_timeout',\n        error_type: 'enrichment_timeout',\n        error_message: errorText,\n        retry_eligible: 'true',\n        contact_search_status: '',\n        contact_search_reason: '',\n        source_row_created_at: String(source.source_row_created_at || ''),\n        source_row_updated_at: String(source.source_row_updated_at || '')\n      },\n      pairedItem: item.pairedItem\n    }];\n  }\n  return [{\n    json: {\n      Id: source.Id,\n      status: 'failed',\n      last_stage: 'enrichment_error',\n      last_error: errorText,\n      notes: errorText,\n      run_id: String(source.run_id || ''),\n      processing_started_at: String(source.processing_started_at || ''),\n      processing_finished_at: finishedAt,\n      last_attempted_at: String(source.last_attempted_at || ''),\n      attempt_count: String(source.attempt_count || '').trim() || '1',\n      status_reason: 'enrichment_error',\n      error_type: 'enrichment_error',\n      error_message: errorText,\n      retry_eligible: genericMaxAttemptsReached ? 'false' : 'true',\n      source_row_created_at: String(source.source_row_created_at || ''),\n      source_row_updated_at: String(source.source_row_updated_at || '')\n    },\n    pairedItem: item.pairedItem\n  }];\n});"
      }
    },
    {
      "id": "patch-enrichment-result",
      "name": "Patch Enrichment Result",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        2300,
        520
      ],
      "parameters": {
        "method": "PATCH",
        "url": "https://nocodb-production-f802.up.railway.app/api/v1/db/data/bulk/noco/pb7f1zou786xyqc/mey3zgihq7o4at9",
        "sendHeaders": false,
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify([$json]) }}",
        "options": {
          "timeout": 30000
        },
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "nocoDbApiToken"
      },
      "credentials": {
        "nocoDbApiToken": {
          "id": "d5Rz1LXeEoHMrZwo",
          "name": "NocoDB Token account"
        }
      },
      "retryOnFail": true,
      "maxTries": 2,
      "waitBetweenTries": 1000
    },
    {
      "id": "claim-discovery-row",
      "name": "Claim Discovery Row",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1000,
        300
      ],
      "parameters": {
        "method": "PATCH",
        "url": "https://nocodb-production-f802.up.railway.app/api/v1/db/data/bulk/noco/pb7f1zou786xyqc/mey3zgihq7o4at9",
        "sendHeaders": false,
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify([{ Id: $json.Id, url_picked: String($json.url_picked || ''), homepage_root_url: String($json.homepage_root_url || ''), best_url: String($json.best_url || ''), canonical_domain: String($json.canonical_domain || ''), status: 'processing', last_stage: 'url_discovery', last_error: '', run_id: String($execution.id || ''), processing_started_at: new Date().toISOString(), processing_finished_at: '', last_attempted_at: new Date().toISOString(), attempt_count: String((Number($json.attempt_count || 0) || 0) + 1), status_reason: 'processing:url_discovery', error_type: '', error_message: '', retry_eligible: 'true', source_row_created_at: String($json.source_row_created_at || $json.CreatedAt || ''), source_row_updated_at: String($json.source_row_updated_at || $json.UpdatedAt || '') }]) }}",
        "options": {
          "timeout": 30000
        },
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "nocoDbApiToken"
      },
      "credentials": {
        "nocoDbApiToken": {
          "id": "d5Rz1LXeEoHMrZwo",
          "name": "NocoDB Token account"
        }
      },
      "retryOnFail": true,
      "maxTries": 2,
      "waitBetweenTries": 1000
    },
    {
      "id": "continue-discovery-claim",
      "name": "Continue Discovery Claim",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1260,
        300
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "function domainFromUrl(value) {\n  const raw = String(value || '').trim();\n  if (!raw) return '';\n  try {\n    const parsed = new URL(/^[a-z][a-z0-9+.-]*:\\/\\//i.test(raw) ? raw : 'https://' + raw);\n    return parsed.hostname.toLowerCase().replace(/^www\\./, '');\n  } catch {\n    return '';\n  }\n}\nfunction rediscoveryExcludedDomain(row) {\n  const reason = String(row.status_reason || '').trim();\n  return reason === 'skipped_url_validation_failed'\n    ? (String(row.canonical_domain || '').trim().toLowerCase() || domainFromUrl(row.url_picked || row.best_url || row.homepage_root_url))\n    : '';\n}\nreturn $input.all().map((item, index) => {\n  const row = item.json || {};\n  const excludedDomain = rediscoveryExcludedDomain(row);\n  const now = new Date().toISOString();\n  const priorAttempt = Number.parseInt(String(row.attempt_count || '0'), 10);\n  const attempt = (Number.isFinite(priorAttempt) ? priorAttempt : 0) + 1;\n  const runId = String(row.run_id || '').trim() || `worker:${Date.now()}:${row.Id}`;\n  return {\n    json: {\n      ...row,\n      status: 'processing',\n      last_stage: 'url_discovery',\n      last_error: '',\n      status_reason: 'processing:url_discovery',\n      excluded_url_domain: excludedDomain,\n      run_id: runId,\n      processing_started_at: now,\n      processing_finished_at: '',\n      last_attempted_at: now,\n      attempt_count: String(attempt),\n      retry_eligible: 'true',\n      error_type: '',\n      error_message: ''\n    },\n    pairedItem: { item: index }\n  };\n});"
      }
    },
    {
      "id": "claim-enrichment-row",
      "name": "Claim Enrichment Row",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1000,
        620
      ],
      "parameters": {
        "method": "PATCH",
        "url": "https://nocodb-production-f802.up.railway.app/api/v1/db/data/bulk/noco/pb7f1zou786xyqc/mey3zgihq7o4at9",
        "sendHeaders": false,
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ (() => { const weakRetry = String($json.automation_decision || '').trim() === 'retry_enrichment_once'; const manualUrl = String($json.manual_url_override || '').trim(); function canonicalDomain(url) { let value = String(url || '').trim().toLowerCase(); if (value.startsWith('http://')) value = value.slice(7); if (value.startsWith('https://')) value = value.slice(8); value = value.split('/')[0].split(':')[0].trim(); if (value.startsWith('www.')) value = value.slice(4); return value; } const manualDomain = canonicalDomain(manualUrl); const patch = { Id: $json.Id, status: 'processing', last_stage: 'crawl', last_error: '', run_id: String($execution.id || ''), processing_started_at: new Date().toISOString(), processing_finished_at: '', last_attempted_at: new Date().toISOString(), attempt_count: String((Number($json.attempt_count || 0) || 0) + 1), status_reason: manualUrl ? 'processing:crawl:manual_url_override' : (weakRetry ? 'processing:crawl:weak_enrichment_retry' : 'processing:crawl'), error_type: '', error_message: '', retry_eligible: 'true', source_row_created_at: String($json.source_row_created_at || $json.CreatedAt || ''), source_row_updated_at: String($json.source_row_updated_at || $json.UpdatedAt || '') }; if (manualUrl) Object.assign(patch, { url_picked: manualUrl, manual_url_override: manualUrl, homepage_root_url: manualUrl, best_url: manualUrl, canonical_domain: manualDomain, status_reason: 'processing:crawl:manual_url_override' }); if (weakRetry) Object.assign(patch, { automation_decision: '', automation_decision_reason: '', pressure_type: '', primary_email_track: '', secondary_email_track: '', email_1_subject: '', email_1_body: '', email_1_llm_rewritten: false, email_2_subject: '', email_2_body: '', email_2_llm_rewritten: false, email_3_subject: '', email_3_body: '', email_3_body_html: '', email_quality_flags: '[]', severe_email_flags: '[]', automation_blockers_json: '[]', automation_advisory_flags_json: '[]', final_send_gate_passed: false, email_send_ready: false }); return JSON.stringify([patch]); })() }}",
        "options": {
          "timeout": 30000
        },
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "nocoDbApiToken"
      },
      "credentials": {
        "nocoDbApiToken": {
          "id": "d5Rz1LXeEoHMrZwo",
          "name": "NocoDB Token account"
        }
      },
      "retryOnFail": true,
      "maxTries": 2,
      "waitBetweenTries": 1000
    },
    {
      "id": "continue-enrichment-claim",
      "name": "Continue Enrichment Claim",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1260,
        620
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "return $input.all().map((item, index) => {\n  const row = item.json || {};\n  const now = new Date().toISOString();\n  const priorAttempt = Number.parseInt(String(row.attempt_count || '0'), 10);\n  const attempt = (Number.isFinite(priorAttempt) ? priorAttempt : 0) + 1;\n  const runId = String(row.run_id || '').trim() || `worker:${Date.now()}:${row.Id}`;\n  const manualUrl = String(row.manual_url_override || '').trim();\n  return {\n    json: {\n      ...row,\n      status: 'processing',\n      last_stage: 'crawl',\n      last_error: '',\n      status_reason: manualUrl ? 'processing:crawl:manual_url_override' : 'processing:crawl',\n      run_id: runId,\n      processing_started_at: now,\n      processing_finished_at: '',\n      last_attempted_at: now,\n      attempt_count: String(attempt),\n      retry_eligible: 'true',\n      error_type: '',\n      error_message: ''\n    },\n    pairedItem: { item: index }\n  };\n});"
      }
    },
    {
      "id": "webhook-contact-search-trigger",
      "name": "Webhook Contact Search Trigger",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [
        220,
        820
      ],
      "parameters": {
        "httpMethod": "POST",
        "path": "rayn-contact-search-batch",
        "responseMode": "onReceived",
        "options": {}
      },
      "webhookId": "4b6d2f56-0e9f-4c57-930c-67e94cf59f84"
    },
    {
      "id": "get-contact-search-rows",
      "name": "Get Contact Search Rows",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        480,
        820
      ],
      "parameters": {
        "method": "GET",
        "url": "={{ (() => { const body = $json.body || {}; const query = $json.query || {}; const rawIds = body.ids || body.row_ids || query.ids || query.row_ids || []; const ids = (Array.isArray(rawIds) ? rawIds : String(rawIds || '').split(',')).map((value) => String(value).trim()).filter(Boolean); const limitValue = ids.length ? ids.length : (body.limit || query.limit || $env.RAYN_CONTACT_SEARCH_BATCH_LIMIT || $env.CONTACT_SEARCH_BATCH_LIMIT || 5); const limit = Math.max(1, Math.min(25, Number(limitValue) || 5)); const base = 'https://nocodb-production-f802.up.railway.app/api/v1/db/data/noco/pb7f1zou786xyqc/mey3zgihq7o4at9'; const fields = 'Id,company_name,best_url,canonical_domain,contact_search_status,contact_search_reason,contact_search_run_id,contact_search_started_at,contact_search_finished_at,selected_contact_name,selected_contact_title,selected_contact_role,selected_contact_email,selected_contact_seniority,selected_contact_confidence,selected_contact_source_url,selected_contact_linkedin_url,validated_email,duplicate_validated_email_of_id,email_candidates_json,email_validation_status,email_validation_summary,email_validation_provider,email_validation_evidence_json,contact_search_evidence_json,send_provider,send_status,sequence_status,instantly_sync_status,source_row_created_at,source_row_updated_at,CreatedAt,UpdatedAt'; const staleMinutes = Math.max(5, Number($env.RAYN_STALE_PROCESSING_MINUTES || 15)); const staleCutoff = encodeURIComponent(new Date(Date.now() - staleMinutes * 60 * 1000).toISOString()); const sendSafeWhere = '(send_provider,neq,instantly)~and(instantly_sync_status,neq,synced)'; const contactReadyWhere = '((contact_search_status,eq,pending)~or((contact_search_status,eq,processing)~and(contact_search_started_at,lt,' + staleCutoff + ')))'; const where = ids.length ? '(Id,in,' + ids.join(',') + ')' : '(status,eq,completed)~and(canonical_domain,notblank)~and(best_url,notblank)~and(duplicate_of_id,blank)~and' + contactReadyWhere + '~and' + sendSafeWhere; return base + '?where=' + encodeURIComponent(where) + '&fields=' + encodeURIComponent(fields) + '&limit=' + String(limit) + '&sort=Id'; })() }}",
        "sendHeaders": false,
        "options": {
          "timeout": 30000
        },
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "nocoDbApiToken"
      },
      "credentials": {
        "nocoDbApiToken": {
          "id": "d5Rz1LXeEoHMrZwo",
          "name": "NocoDB Token account"
        }
      },
      "retryOnFail": true,
      "maxTries": 2,
      "waitBetweenTries": 1000
    },
    {
      "id": "rows-to-contact-search-items",
      "name": "Rows To Contact Search Items",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        740,
        820
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const rows = Array.isArray($json.list) ? $json.list : [];\nreturn rows\n  .filter((row) => row && row.Id && String(row.best_url || '').trim() && String(row.canonical_domain || '').trim())\n  .map((row, index) => ({\n    json: {\n      ...row,\n      contact_search_claim_source: 'n8n_contact_row_job'\n    },\n    pairedItem: { item: index }\n  }));"
      }
    },
    {
      "id": "claim-contact-search-row",
      "name": "Claim Contact Search Row",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1000,
        820
      ],
      "parameters": {
        "method": "PATCH",
        "url": "https://nocodb-production-f802.up.railway.app/api/v1/db/data/bulk/noco/pb7f1zou786xyqc/mey3zgihq7o4at9",
        "sendHeaders": false,
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify([{ Id: $json.Id, contact_search_status: 'processing', contact_search_reason: 'processing:contact_search_claimed', contact_search_started_at: new Date().toISOString(), contact_search_finished_at: '', contact_search_run_id: 'n8n:' + String($execution.id || '') + ':' + String($json.Id || ''), selected_contact_name: '', selected_contact_title: '', selected_contact_role: '', selected_contact_email: '', selected_contact_seniority: '', selected_contact_confidence: '', selected_contact_source_url: '', selected_contact_linkedin_url: '', validated_email: '', duplicate_validated_email_of_id: '', email_candidates_json: '', email_validation_status: '', email_validation_summary: '', email_validation_provider: '', email_validation_evidence_json: '', contact_search_evidence_json: '', source_row_created_at: String($json.source_row_created_at || $json.CreatedAt || ''), source_row_updated_at: String($json.source_row_updated_at || $json.UpdatedAt || '') }]) }}",
        "options": {
          "timeout": 30000
        },
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "nocoDbApiToken"
      },
      "credentials": {
        "nocoDbApiToken": {
          "id": "d5Rz1LXeEoHMrZwo",
          "name": "NocoDB Token account"
        }
      },
      "retryOnFail": true,
      "maxTries": 2,
      "waitBetweenTries": 1000
    },
    {
      "id": "continue-contact-search-claim",
      "name": "Continue Contact Search Claim",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1260,
        820
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const sourceItems = $('Rows To Contact Search Items').all();\nreturn $input.all().map((item, index) => {\n  const source = (sourceItems[index] && sourceItems[index].json) || {};\n  const patchResult = Array.isArray(item.json) ? (item.json[0] || {}) : (item.json || {});\n  return {\n    json: {\n      ...source,\n      ...patchResult,\n      Id: patchResult.Id || source.Id\n    },\n    pairedItem: item.pairedItem || { item: index }\n  };\n});"
      }
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ String($env.CRAWL4AI_SCRAPER_URL || 'https://n8n-rayn-workflows-production.up.railway.app/scrape').replace(/\\/scrape$/, '/contact-enrich-row') }}",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Content-Type",
              "value": "application/json"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ row_id: $json.Id, reset_provider_health: false, validate_email: true }) }}",
        "options": {
          "timeout": 30000
        }
      },
      "id": "8a6c96d0-6823-4fb8-bcf8-f4c61c872077",
      "name": "Run Contact Search Row",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1520,
        820
      ],
      "continueOnFail": true
    },
    {
      "id": "build-contact-search-transport-patches",
      "name": "Build Contact Search Transport Patches",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1780,
        820
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const sources = $('Continue Contact Search Claim').all();\nfunction stringify(value) {\n  try {\n    if (value == null) return '';\n    if (typeof value === 'string') return value;\n    return JSON.stringify(value);\n  } catch {\n    return String(value || '');\n  }\n}\nfunction messageFrom(payload) {\n  return stringify(payload?.error || payload?.message || payload?.reason || payload);\n}\nreturn $input.all().flatMap((item, index) => {\n  const source = (sources[index] && sources[index].json) || {};\n  const payload = item.json || {};\n  const accepted = payload.accepted === true && payload.ok !== false && !payload.error;\n  if (accepted) return [];\n  const message = messageFrom(payload) || 'contact search row enqueue failed';\n  return [{\n    json: {\n      Id: source.Id,\n      contact_search_status: 'failed_retryable',\n      contact_search_reason: 'contact_search_transport_timeout',\n      contact_search_finished_at: new Date().toISOString(),\n      contact_search_evidence_json: JSON.stringify({ source: 'n8n_contact_row_job', error: message, response: payload }),\n      email_candidates_json: '[]',\n      email_validation_provider: 'anymail_finder',\n      email_validation_status: '',\n      email_validation_summary: message,\n      email_validation_evidence_json: JSON.stringify({ error: message })\n    },\n    pairedItem: item.pairedItem || { item: index }\n  }];\n});"
      }
    },
    {
      "id": "patch-contact-search-transport-failure",
      "name": "Patch Contact Search Transport Failure",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        2040,
        820
      ],
      "parameters": {
        "method": "PATCH",
        "url": "https://nocodb-production-f802.up.railway.app/api/v1/db/data/bulk/noco/pb7f1zou786xyqc/mey3zgihq7o4at9",
        "sendHeaders": false,
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify([$json]) }}",
        "options": {
          "timeout": 30000
        },
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "nocoDbApiToken"
      },
      "credentials": {
        "nocoDbApiToken": {
          "id": "d5Rz1LXeEoHMrZwo",
          "name": "NocoDB Token account"
        }
      },
      "retryOnFail": true,
      "maxTries": 2,
      "waitBetweenTries": 1000
    }
  ],
  "connections": {
    "Manual Trigger": {
      "main": [
        [
          {
            "node": "Get Blank URL Picked Rows",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Webhook Trigger": {
      "main": [
        [
          {
            "node": "Webhook To Item",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Webhook Batch Trigger": {
      "main": [
        [
          {
            "node": "Get Blank URL Picked Rows",
            "type": "main",
            "index": 0
          },
          {
            "node": "Get Enrichment Rows",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Get Blank URL Picked Rows": {
      "main": [
        [
          {
            "node": "Rows To Items",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Get Enrichment Rows": {
      "main": [
        [
          {
            "node": "Rows To Enrichment Items",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Rows To Items": {
      "main": [
        [
          {
            "node": "Claim Discovery Row",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Rows To Enrichment Items": {
      "main": [
        [
          {
            "node": "Claim Enrichment Row",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Webhook To Item": {
      "main": [
        [
          {
            "node": "Claim Discovery Row",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "OpenRouter URL Pick": {
      "main": [
        [
          {
            "node": "Parse URL Pick",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Parse URL Pick": {
      "main": [
        [
          {
            "node": "Find Canonical Duplicate",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Find Canonical Duplicate": {
      "main": [
        [
          {
            "node": "Apply Dedupe Result",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Apply Dedupe Result": {
      "main": [
        [
          {
            "node": "Patch URL Picked",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Prepare Public Enrichment": {
      "main": [
        [
          {
            "node": "Crawl4AI Public Enrich",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Crawl4AI Public Enrich": {
      "main": [
        [
          {
            "node": "Prepare Enrichment Patch",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Prepare Enrichment Patch": {
      "main": [
        [
          {
            "node": "Patch Enrichment Result",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Claim Discovery Row": {
      "main": [
        [
          {
            "node": "Continue Discovery Claim",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Continue Discovery Claim": {
      "main": [
        [
          {
            "node": "Build URL Discovery Query",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Claim Enrichment Row": {
      "main": [
        [
          {
            "node": "Continue Enrichment Claim",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Continue Enrichment Claim": {
      "main": [
        [
          {
            "node": "Prepare Public Enrichment",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Patch URL Picked": {
      "main": [
        []
      ]
    },
    "Webhook Contact Search Trigger": {
      "main": [
        [
          {
            "node": "Get Contact Search Rows",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Get Contact Search Rows": {
      "main": [
        [
          {
            "node": "Rows To Contact Search Items",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Rows To Contact Search Items": {
      "main": [
        [
          {
            "node": "Claim Contact Search Row",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Claim Contact Search Row": {
      "main": [
        [
          {
            "node": "Continue Contact Search Claim",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Continue Contact Search Claim": {
      "main": [
        [
          {
            "node": "Run Contact Search Row",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Run Contact Search Row": {
      "main": [
        [
          {
            "node": "Build Contact Search Transport Patches",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build Contact Search Transport Patches": {
      "main": [
        [
          {
            "node": "Patch Contact Search Transport Failure",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build URL Discovery Query": {
      "main": [
        [
          {
            "node": "Search URL Discovery Serper",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Prepare URL Discovery Pick": {
      "main": [
        [
          {
            "node": "OpenRouter URL Pick",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Search URL Discovery Serper": {
      "main": [
        [
          {
            "node": "Prepare URL Discovery Pick",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "tags": []
}
